# PAPER OUTLINE — binding on the assembler (revised for "the body is the plan")

Deliverables: `docs/CEQ_SHAPE.md`, `docs/CEQ_SHAPE.tex`, `docs/CEQ_SHAPE.pdf`,
`docs/references.bib`, `docs/PLAN.md` (the standalone roadmap, identical in content to
section 5 plus the apparatus tables it needs).

Target length: the plan is the bulk. Rough budget of the md: front matter 4 %,
shape + obstructions 14 %, record (proved / refuted / measured, compressed) 10 %,
**programme 50 %**, apparatus 12 %, prior art 5 %, limits + references 5 %.

## Front matter
Title; attribution line; abstract (from `sec_intro_draft.md`); keywords; a
one-paragraph "how to read the numbers" (evidence classes).

## 1. Introduction  (`sec_intro_draft.md`, use as written, light edits only)

## 2. The shape  (`judge/sec_shape.md`; ledger `judge/proposition_ledger.md`)
2.1 Definitions (family, regimes S and N, boundary rows with the goal set and the BOS
convention, horizon dial, the read as a mixture). 2.2 Propositions with proofs or Lean
names and RUN numbers. 2.3 What it computes (consequence, committors, safest move,
transient state). 2.4 Occupancy paragraph BEFORE the delta paragraph. 2.5 The
proposition ledger as a table (status tokens; OPEN rows point at the plan card that
discharges them).

## 3. Why per-row softmax cannot occupy this ground  (`judge/sec_obstructions.md`)
Short. The four obstructions with hypotheses; the DET-class relocation; the three
skylines; the sentence the paper does not write.

## 4. What the record proved, measured and refuted  (compressed from
`sections/sec_proved.md`, `sec_measured.md`, `sec_refuted.md`, `sec_state.md`)
4.1 Proved: the load-bearing Lean declarations in one table (name, file:line,
hypotheses, certifies, instance, census). 4.2 Measured: the identity binds, the
deciding measurement R1 and its re-take (the bimodal split, never the mean alone),
the device certificate, the calibration column. 4.3 Refuted: the dead programmes with
the numbers that killed them; the mechanism taxonomy by class with the ten most likely
to recur in this programme and the rule each imposes; the eight record corrections
found in this paper. 4.4 Where round 15 left the campaign (scoreboard 2 of 44; the leap
still owed; the laws in force, one line each).

## 5. THE PROGRAMME  (`plan/PLAN.md`, verbatim structure)
5.0 The one-sentence shape and the one-sentence claim it can at best earn. 5.1 How to
use the plan: phases, gates, the DAG, the five cheapest decisive cards, the
first-evening list. 5.2 The laws in force. 5.3 Phases 0..N with their gates and
milestone cards (every field of the card format). 5.4 The critical path and the
evenings count, shown. 5.5 The predictions ledger with counters. 5.6 The standing
attacks. 5.7 The cost table by phase and the Kaggle gate. 5.8 The Lean ledger.
5.9 The leap-dossier discipline. 5.10 What dies if what. 5.11 Deliverables, the
return report, and how to keep the plan current.

## 6. The apparatus  (`judge/sec_apparatus.md`; ledger `judge/bind_ledger.md`)
Beds (with F1/F2 conventions), labels, oracles and where they run, floors, skylines
and fellow approximators, control arms, metrics, domain censuses, binds with planted
negatives, certificates, the identity manifest, the pricing rule.

## 7. Prior art and occupancy  (from the eight sweeps)
One table: lineage, owner(s), bib key(s), what it owns, what it leaves open; then the
NOT-FOUND list with the queries run; then the record's own prior-art defect (the
filter that hid ChaCAL, V-7 on search).

## 8. Limits  (once)
Including: no trained CEQ arm; the Peng inequality at s = 64; the Cheeger
restatement owed; Misra et al. on multichain safety MDPs; solve_triangular's
undocumented determinism; the Mapper DOI [U]; the 38 identifier-less references;
the session-cap failure of the first design pass (two of three designs and no
refutations reached the first synthesis; the second pass is what this paper reports).

## References  (`references.bib`, canonical keys only; `bib_audit.md` summarised in
Limits)

## Appendix A. The evidence tables in full (measured record, struck constants, the
corrections index rows cited).
## Appendix B. Vocabulary — the field's names for the author's constructions (the
mentor table from THESIS_CORRECTIONS.md C10).

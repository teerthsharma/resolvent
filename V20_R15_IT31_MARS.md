# V20 R15 it.31 — MARS

**8 attacked, 7 struck.** One instrument, `tests/mars_v20/test_v20_r15_it31_the_repairs_of_it29_it30.py`,
digest `ba3eab6437925ff5`, regenerated by:

```
python -m pytest tests/mars_v20/test_v20_r15_it31_the_repairs_of_it29_it30.py -q --no-header -p no:cacheprovider
```

reading `7 failed, 1 passed in 3.52s`. All readings 2026-09-02. **No mutations were
made, so none were reverted.** Digests of every file read, at time of reading:

| file | digest |
|---|---|
| `tests/mars_v20/test_v20_r15_it31_the_repairs_of_it29_it30.py` | `ba3eab6437925ff5` |
| `tests/mercury/test_v20_r15_it29_withdrawals.py` | `b2a9cc0fb1c46973` |
| `tests/mercury/phase_c_price.py` | `2f4142cfdf3a4a8e` |
| `tests/mercury/test_v20_r15_it13_phase_c_price.py` | `106160dd6c4f93b0` |
| `V20_R15_THEORY_TABLE.md` | `942e4208893444cd` |
| `V20_R15_JOURNAL.md` | `2bdcfc57a332bbff` |

---

## §1 — THE REPLACEMENT TIER RULE. **STRUCK, on its own certified example.**

The it.28 rule was withdrawn because `NO git writes` and *"only tier 1 can be
refuted"* compose to nothing being refutable. **Replaced:** *tier 1 = a file at a
stated path with a published digest and a regenerating command, committed or not*
(`V20_R15_IT29_INSPECTOR.md:414-419`).

The brief asks: **does the new rule make any currently-unrefutable number
refutable, or does it relabel them?** It relabels. The proof is the round's own
newest control node, and the rule certifies it.

`tests/mercury/test_v20_r15_it29_withdrawals.py` satisfies the replacement rule in
**full** — stated path, digest `b2a9cc0fb1c46973` published twice
(`V20_R15_IT29_MERCURY.md:15` and `:274`), and a pytest command. MERCURY published
`4 RED / 4 GREEN`. The INSPECTOR read `5 failed, 3 passed` twenty-five minutes
later at an **unchanged digest**. It reads the second answer now.

**MARS-31-A. RED:**

```
E  AssertionError: the replacement tier rule certifies this file at its published
   digest; MERCURY published 4 RED / 4 GREEN against digest b2a9cc0fb1c46973;
   the same bytes now read: '5 failed, 3 passed in 0.61s'
E  assert '4 failed, 4 passed' in '5 failed, 3 passed in 0.61s'
```

**A digest pins the instrument. It does not pin the result.** For an instrument
whose assertions read the working tree — which is every R15 instrument — the
regenerating command is a function of two arguments and the rule publishes one.
The old rule was unsatisfiable; the new rule is **satisfiable and does not
discriminate.** `62/63/64` remains unrefutable for a reason the new rule does not
address: not that no file existed, but that no *population* was stated.

**And the two results the replacement is argued from do not support it.** `26 of 41`
against `29 of 44` resolved because both grammars were runnable **over the same
frozen population** — the resolution came from the population being shared, not
from a file existing. The replacement rule reads the wrong variable off its own
worked example.

> **REPLACEMENT ROUTE (MARS-31-R1).** Tier 1 = *a file at a stated path, a
> published digest, a regenerating command, **and a published digest of the corpus
> subset the command reads.*** Three of those four already exist in every filing.
> The fourth is the one that was missing when the withdrawals node moved, and it is
> mechanical: the node names its own inputs, so it can hash them. A number is
> refutable when a second office can reconstruct **both** arguments.

**On the declined promotion pass:** *"it recentralises the collision risk the law
spreads"* is **a preference, not a measurement, and this office says so on the
record.** The round has one measured datum on coordinator-centralised edits —
§4.3, one pass, four nodes broken, byte-exact revert — and **zero** on the
distributed alternative. A single trial with a clean recovery is not a rate. The
claim as written compares a measured cost against an unmeasured one and reports
the comparison as a finding.

> **ROUTE (MARS-31-R2).** State it as a preference under uncertainty and record the
> denominator: *"one centralised pass, four nodes broken, recovered; no distributed
> pass has been run."* That sentence is defensible. The current one is not.

---

## §2 — RULING `M-30`. **STRUCK THREE WAYS. The band and the point are one object,
and the point is the number the same ruling withdraws.**

`_sweep(e) = 3 · Σ_{S∈{32,64,128}} (S/64)^e · Σ means`. The published band and
point are `_sweep(1.0)`, `_sweep(2.0)`, `_sweep(3.0)` on it.13's **re-measured**
means — the exact object `M-30a` disqualifies.

| e | re-measured `16.161/1.780/1.681` | literal `16.16/1.78/1.68` |
|---|---|---|
| 1 | `206.031` | `206.010` |
| 2 | **`309.047`** — withdrawn by `M-30a` | **`309.015`** — vindicated by `M-30a` |
| 3 | `537.152` | `537.098` |

**MARS-31-B. The prescribed point is the withdrawn number. RED:**

```
E  AssertionError: M-30's prescribed point must come from the formula it vindicates,
   not from the object it withdraws: formula=309.015, withdrawn=309.047, both round
   to 309.0
```

`M-30` prescribes two edits and calls them *"not the same edit"*. Applying `:354`
(`~275 GPU-s` → `206–537 GPU-s (point 309.0)`) **reinstates at one decimal the
number `:184` is being corrected for carrying at three.** The repair for a
mis-attribution quotes the mis-attributed value.

**MARS-31-B2. The band cannot separate the two objects the ruling exists to
separate. RED:**

```
E  AssertionError: a band both objects produce is not a repair for confusing them:
   re-measured -> 206-537 (206.031, 537.152); literal -> 206-537 (206.010, 537.098)
```

Both families print `206–537`. **The prescribed honest quote erases the entire
distinction `M-30` was made to draw.** A reader given `206–537 (point 309.0)`
cannot recover which of the three numbers the round is standing behind — which is
the defect that produced `275` in the first place.

**MARS-31-B3. Where the endpoints come from, and why the point is inside them.
RED:**

```
E  AssertionError: band and point are one function at three exponents -- containment
   is guaranteed by monotonicity in e, not measured: 206.031 / 309.047 / 537.152
```

The brief's question, answered: **the endpoints are `_sweep` at e=1 and e=3 on the
re-measured means, and the point is `_sweep` at e=2 on the same means.** The point
is inside the band because `(S/64)^e` is monotone in `e` and `2 ∈ (1,3)` — **by
construction, in every possible world.** Not for the reason a `band + point`
notation claims, which is that an independent central estimate fell inside an
independently bounded range. **Three readings of one function are one measurement.**
The containment carries zero information about the price.

> **REPLACEMENT ROUTE (MARS-31-R3).** Quote the price as
> `_sweep(e) on MEAN_SECS, e ∈ [1,3] unmeasured, = 206–537 GPU-s`, with **no point
> at all.** If a point is required for a leap decision, the leap needs `e`, and `e`
> is measurable at `0 GPU-s` from the three `S` values the round has already banked.
> **That measurement, not a third number, is the repair.**

---

## §3 — THE it.30 WITHDRAWALS INHERIT THE `62` REACH DEFECT. **STRUCK, both.**

The nurse's rule from the `62`: *a withdrawal published in one file is not a
withdrawal.* `M-30a` and the `275` withdrawal were filed one iteration later and
have the same shape.

**MARS-31-C. RED:**

```
E  AssertionError: M-30a names one address; 2 others still assert it:
   ['tests/mercury/test_v20_r15_it13_phase_c_price.py:48',
    'tests/mercury/test_v20_r15_it13_phase_c_price.py:55']
```

`M-30a` names `V20_R15_THEORY_TABLE.md:184`. The attribution stands live at two
more — **inside a runnable instrument**, where `:55` carries the comment
`# S^2, the assumed law` and the docstring at `:47` says `309.02` while the
assertion says `309.047`. `M-30` identifies that tell and does not repair it.
**Worse than the `62`: the `62` survived in prose; this survives in a green
assertion.**

**MARS-31-C2. RED:**

```
E  AssertionError: a price the round rules is produced by nothing is still the quoted
   price at V20_R15_THEORY_TABLE.md:[354]
```

`275` is ruled *produced by nothing* at `V20_R15_IT30_MERCURY.md:86`. `:354` is the
address the `L-9` leap acts on. **The edit is prescribed, not applied.**

> **ROUTE (MARS-31-R4).** Every withdrawal ships an **address list produced by a
> grep, not by memory**, and the withdrawing node asserts the list is empty. That
> is the `M-29a` pattern which the nurse confirmed worked — two explicit addresses,
> propagated — applied to the two that did not.

---

## §4 — SATURN's PATHS-ONLY BOUNDARY, AND THE COUNT RETIREMENT. **NOT FILED; the
class struck in advance.**

SATURN has filed nothing after `test_v20_r15_it29_planted_negative_rederived.py`,
so the count retirement could not be attacked as shipped. **The class was struck
against the round's own newest record instead.**

**MARS-31-D. RED:**

```
E  AssertionError: the journal's own [RUN] ls is falsified by the corpus it counts:
   the IT30 partition grew after the it.30 census closed it
```

The it.30 entry records as measured fact, with a `[RUN] ls`, that
`V20_R15_IT30_JUPITER.md` does not exist. **It exists.** This is the third instance
of the class in three iterations — JUPITER's `25 → 28`, SATURN's `750 → 766 → 784`,
and now the journal's own existence claim — and it is the one that matters for
`S5`, because `S5`'s survivor was *"the closed subset `IT23-28` stayed exactly
`163`."*

**A partition keyed on iteration number is closed only while every office files
inside its cap, and the round has an instance from last iteration of one that did
not.** `IT30` is not closed. `IT23-28` is closed by the same claim and the same
evidence, which is to say by nothing.

Against the anticipated `>= N` route: **a monotone bound survives growth and cannot
detect a deletion** — struck in the index recipe at it.25, and the count was kept
for that reason. **A partitioned count over a closed subset is only as closed as
the claim that it is closed, and `MARS-31-D` is the measurement that the claim is
false for at least one partition of this round.**

> **REPLACEMENT ROUTE (MARS-31-R5).** Retire the count and keep the **set**: assert
> a sorted tuple of relative paths, not `len()`. It detects growth, detects
> deletion, names the delta in the failure message, and needs no closure claim.
> Where a number must be published, publish it as `N as of <digest of the sorted
> path list>` — the same `R1` fix, one level down.

**NOT REACHED:** the `paths-only`/`prose-only` discrimination resting on `ceq/`
symbol names is unattacked. **This office did not open it.** The attack that was
being built: `S1` measures an empty anchor set — `path_product` and `zero_hop_mask`
return zero hits in `ceq/arm_pl.py` — and *derives identically to control* is
therefore a statement about an empty set, which is vacuously true for **every**
rewrite that omits those two symbols, not only a paths-only one. **Unverified.**

---

## §5 — THE COORDINATOR. **`do not edit` is CORRECT. The radius is COMPLETE.**

Verified: **14 `.py` files under `tests/` read `V20_R15_JOURNAL.md`** (`__pycache__`
excluded) — his figure, exactly, across `jupiter` (6), `mercury` (3), `mars_v20`
(2), `saturn` (3). **He did not undercount.**

And **`do not edit` is not an over-correction**, on his own two grounds and one he
did not state:

1. Both journal `62` sites are inside **filed entry bodies**. An office's filed text
   is not the coordinator's to amend, which is a boundary, not a caution.
2. A `C37` row moves `dead_literals()` under JUPITER's frozen counts **mid-flight** —
   and JUPITER has not filed, so his it.30 nodes have not been re-taken.
3. **The one he did not state, and it is the strongest:** `MARS-31-A` shows the
   journal is read by 14 nodes whose published results are pinned to **no corpus
   digest**. Any journal edit invalidates 14 published compositions with no way to
   tell which. **He computed the radius and got the right answer for two reasons;
   there were three.**

The `62` propagation is real and still owed. It is owed **to the offices that filed
those entries**, not to the coordinator.

---

## §6 — DISPOSITION

**8 attacked, 7 struck, 1 verified for the defence.**

| # | target | verdict |
|---|---|---|
| A | replacement tier rule certifies an irreproducible instrument | **STRUCK** |
| A′ | *"recentralises the collision risk"* as a measured claim | **STRUCK** — preference, denominator 1 vs 0 |
| B | `M-30` point is the number `M-30a` withdraws | **STRUCK** |
| B2 | `206–537` cannot separate the two objects | **STRUCK** |
| B3 | point inside band by monotonicity, not agreement | **STRUCK** |
| C | `M-30a` reach — 2 live addresses in a runnable node | **STRUCK** |
| C2 | `275` reach — `:354` unrepaired | **STRUCK** |
| D | iteration-keyed partitions are not closed | **STRUCK** |
| — | coordinator's radius and `do not edit` | **UPHELD**, and understated by one ground |

**RULING ON THE REPLACEMENT TIER RULE.** The withdrawal of it.28 is right. The
replacement is **not wrong, and not sufficient** — it names three of the four things
a refutable number needs and omits the one whose absence the round measured twice in
one window. **It relabels 85 files as tier 1 without making one number refutable
that was not refutable before.** `MARS-31-R1` is the missing clause and costs
nothing: every node already names its own inputs.

**NOT REACHED, named:** SATURN's `paths-only` symbol-name discrimination (attack
sketched in §4, unverified); the six-range sweep and the `L-13` `572-600` overrun;
JUPITER's it.30 phrase grammar and its blast radius, unfiled at the time of this
attack; §0 and §1 of the theory table, still never swept by anyone.

**TREE STATEMENT.** `HEAD` `207e7b9`, unmoved; no git operation was performed by
this office and none appears in the reflog; `git reflog -1` reads
`207e7b9 HEAD@{0}: reset: moving to HEAD` — the coordinator's it.29 revert, still the
last entry. `git status --porcelain` read **175** before this filing and **177** after,
**+2, exactly this office's two files.**
**This office wrote exactly two files:**
`tests/mars_v20/test_v20_r15_it31_the_repairs_of_it29_it30.py` (`ba3eab6437925ff5`)
and `V20_R15_IT31_MARS.md`. **No mutation was made to any file of any other office,
so no revert was necessary; the digest table above is the proof, and every digest in
it that was published by another office matches that office's published value.**

**Twenty minutes. No git writes. Nothing touched Kaggle.**

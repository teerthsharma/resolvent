........................................................................ [ 13%]
..........FFFFFFFFFF............FF..................F................... [ 27%]
........................................................................ [ 41%]
...F.................................................................... [ 55%]
........................................................................ [ 69%]
........................................................................ [ 83%]
................................................................F....... [ 97%]
............                                                             [100%]
================================== FAILURES ===================================
_ test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_ceq_hub_package.py] _

path = 'tests/chase/test_ceq_hub_package.py'

    @pytest.mark.parametrize("path", BARE_CONFTEST_IMPORTERS)
    def test_no_test_file_imports_conftest_as_a_bare_module(path: str):
        """The mechanism, per file, so a partial repair is visible as a partial pass."""
        found = resolve(path)
        if found is None:
            pytest.fail(
                f"{path} resolves to no file, at its own path or under attic/. "
                "A named offender that exists nowhere is not a repair; say where it went."
            )
        src = found.read_text(encoding="utf-8")
        # `.lstrip()` before matching. Without it this assertion saw only UNINDENTED
        # imports while `bare_conftest_importers()` scans with `^\s*`, so five of the
        # ten parametrised files were collected and could never fail -- the selector
        # and the check disagreeing about what counts, which makes half the cases
        # vacuous. A function-local `from conftest import X` resolves through
        # `sys.path` at call time exactly as a module-level one does.
        offenders = [
            line.strip()
            for line in src.splitlines()
            if line.lstrip().startswith(("from conftest import", "import conftest"))
        ]
>       assert not offenders, (
            f"{path} reaches conftest as a top-level module: {offenders}. "
            "With only 2 of 26 test dirs carrying __init__.py, that name resolves by "
            "collection order."
        )
E       AssertionError: tests/chase/test_ceq_hub_package.py reaches conftest as a top-level module: ['from conftest import run_isolated']. With only 2 of 26 test dirs carrying __init__.py, that name resolves by collection order.
E       assert not ['from conftest import run_isolated']

tests\loop\test_conftest_import_is_order_dependent.py:208: AssertionError
_ test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_hf_shipping.py] _

path = 'tests/chase/test_hf_shipping.py'

    @pytest.mark.parametrize("path", BARE_CONFTEST_IMPORTERS)
    def test_no_test_file_imports_conftest_as_a_bare_module(path: str):
        """The mechanism, per file, so a partial repair is visible as a partial pass."""
        found = resolve(path)
        if found is None:
            pytest.fail(
                f"{path} resolves to no file, at its own path or under attic/. "
                "A named offender that exists nowhere is not a repair; say where it went."
            )
        src = found.read_text(encoding="utf-8")
        # `.lstrip()` before matching. Without it this assertion saw only UNINDENTED
        # imports while `bare_conftest_importers()` scans with `^\s*`, so five of the
        # ten parametrised files were collected and could never fail -- the selector
        # and the check disagreeing about what counts, which makes half the cases
        # vacuous. A function-local `from conftest import X` resolves through
        # `sys.path` at call time exactly as a module-level one does.
        offenders = [
            line.strip()
            for line in src.splitlines()
            if line.lstrip().startswith(("from conftest import", "import conftest"))
        ]
>       assert not offenders, (
            f"{path} reaches conftest as a top-level module: {offenders}. "
            "With only 2 of 26 test dirs carrying __init__.py, that name resolves by "
            "collection order."
        )
E       AssertionError: tests/chase/test_hf_shipping.py reaches conftest as a top-level module: ['from conftest import HAS_CUDA']. With only 2 of 26 test dirs carrying __init__.py, that name resolves by collection order.
E       assert not ['from conftest import HAS_CUDA']

tests\loop\test_conftest_import_is_order_dependent.py:208: AssertionError
_ test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_hub_package_hardening.py] _

path = 'tests/chase/test_hub_package_hardening.py'

    @pytest.mark.parametrize("path", BARE_CONFTEST_IMPORTERS)
    def test_no_test_file_imports_conftest_as_a_bare_module(path: str):
        """The mechanism, per file, so a partial repair is visible as a partial pass."""
        found = resolve(path)
        if found is None:
            pytest.fail(
                f"{path} resolves to no file, at its own path or under attic/. "
                "A named offender that exists nowhere is not a repair; say where it went."
            )
        src = found.read_text(encoding="utf-8")
        # `.lstrip()` before matching. Without it this assertion saw only UNINDENTED
        # imports while `bare_conftest_importers()` scans with `^\s*`, so five of the
        # ten parametrised files were collected and could never fail -- the selector
        # and the check disagreeing about what counts, which makes half the cases
        # vacuous. A function-local `from conftest import X` resolves through
        # `sys.path` at call time exactly as a module-level one does.
        offenders = [
            line.strip()
            for line in src.splitlines()
            if line.lstrip().startswith(("from conftest import", "import conftest"))
        ]
>       assert not offenders, (
            f"{path} reaches conftest as a top-level module: {offenders}. "
            "With only 2 of 26 test dirs carrying __init__.py, that name resolves by "
            "collection order."
        )
E       AssertionError: tests/chase/test_hub_package_hardening.py reaches conftest as a top-level module: ['from conftest import run_isolated']. With only 2 of 26 test dirs carrying __init__.py, that name resolves by collection order.
E       assert not ['from conftest import run_isolated']

tests\loop\test_conftest_import_is_order_dependent.py:208: AssertionError
_ test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_kernel_contracts.py] _

path = 'tests/chase/test_kernel_contracts.py'

    @pytest.mark.parametrize("path", BARE_CONFTEST_IMPORTERS)
    def test_no_test_file_imports_conftest_as_a_bare_module(path: str):
        """The mechanism, per file, so a partial repair is visible as a partial pass."""
        found = resolve(path)
        if found is None:
            pytest.fail(
                f"{path} resolves to no file, at its own path or under attic/. "
                "A named offender that exists nowhere is not a repair; say where it went."
            )
        src = found.read_text(encoding="utf-8")
        # `.lstrip()` before matching. Without it this assertion saw only UNINDENTED
        # imports while `bare_conftest_importers()` scans with `^\s*`, so five of the
        # ten parametrised files were collected and could never fail -- the selector
        # and the check disagreeing about what counts, which makes half the cases
        # vacuous. A function-local `from conftest import X` resolves through
        # `sys.path` at call time exactly as a module-level one does.
        offenders = [
            line.strip()
            for line in src.splitlines()
            if line.lstrip().startswith(("from conftest import", "import conftest"))
        ]
>       assert not offenders, (
            f"{path} reaches conftest as a top-level module: {offenders}. "
            "With only 2 of 26 test dirs carrying __init__.py, that name resolves by "
            "collection order."
        )
E       AssertionError: tests/chase/test_kernel_contracts.py reaches conftest as a top-level module: ['from conftest import run_isolated  # noqa: E402', 'from conftest import HAS_CUDA  # noqa: E402']. With only 2 of 26 test dirs carrying __init__.py, that name resolves by collection order.
E       assert not ['from conftest import run_isolated  # noqa: E402', 'from conftest import HAS_CUDA  # noqa: E402']

tests\loop\test_conftest_import_is_order_dependent.py:208: AssertionError
_ test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_rollback_flex_attention.py] _

path = 'tests/chase/test_rollback_flex_attention.py'

    @pytest.mark.parametrize("path", BARE_CONFTEST_IMPORTERS)
    def test_no_test_file_imports_conftest_as_a_bare_module(path: str):
        """The mechanism, per file, so a partial repair is visible as a partial pass."""
        found = resolve(path)
        if found is None:
            pytest.fail(
                f"{path} resolves to no file, at its own path or under attic/. "
                "A named offender that exists nowhere is not a repair; say where it went."
            )
        src = found.read_text(encoding="utf-8")
        # `.lstrip()` before matching. Without it this assertion saw only UNINDENTED
        # imports while `bare_conftest_importers()` scans with `^\s*`, so five of the
        # ten parametrised files were collected and could never fail -- the selector
        # and the check disagreeing about what counts, which makes half the cases
        # vacuous. A function-local `from conftest import X` resolves through
        # `sys.path` at call time exactly as a module-level one does.
        offenders = [
            line.strip()
            for line in src.splitlines()
            if line.lstrip().startswith(("from conftest import", "import conftest"))
        ]
>       assert not offenders, (
            f"{path} reaches conftest as a top-level module: {offenders}. "
            "With only 2 of 26 test dirs carrying __init__.py, that name resolves by "
            "collection order."
        )
E       AssertionError: tests/chase/test_rollback_flex_attention.py reaches conftest as a top-level module: ['from conftest import HAS_CUDA  # noqa: E402']. With only 2 of 26 test dirs carrying __init__.py, that name resolves by collection order.
E       assert not ['from conftest import HAS_CUDA  # noqa: E402']

tests\loop\test_conftest_import_is_order_dependent.py:208: AssertionError
_ test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_schedule_rebuild.py] _

path = 'tests/chase/test_schedule_rebuild.py'

    @pytest.mark.parametrize("path", BARE_CONFTEST_IMPORTERS)
    def test_no_test_file_imports_conftest_as_a_bare_module(path: str):
        """The mechanism, per file, so a partial repair is visible as a partial pass."""
        found = resolve(path)
        if found is None:
            pytest.fail(
                f"{path} resolves to no file, at its own path or under attic/. "
                "A named offender that exists nowhere is not a repair; say where it went."
            )
        src = found.read_text(encoding="utf-8")
        # `.lstrip()` before matching. Without it this assertion saw only UNINDENTED
        # imports while `bare_conftest_importers()` scans with `^\s*`, so five of the
        # ten parametrised files were collected and could never fail -- the selector
        # and the check disagreeing about what counts, which makes half the cases
        # vacuous. A function-local `from conftest import X` resolves through
        # `sys.path` at call time exactly as a module-level one does.
        offenders = [
            line.strip()
            for line in src.splitlines()
            if line.lstrip().startswith(("from conftest import", "import conftest"))
        ]
>       assert not offenders, (
            f"{path} reaches conftest as a top-level module: {offenders}. "
            "With only 2 of 26 test dirs carrying __init__.py, that name resolves by "
            "collection order."
        )
E       AssertionError: tests/chase/test_schedule_rebuild.py reaches conftest as a top-level module: ['from conftest import HAS_CUDA']. With only 2 of 26 test dirs carrying __init__.py, that name resolves by collection order.
E       assert not ['from conftest import HAS_CUDA']

tests\loop\test_conftest_import_is_order_dependent.py:208: AssertionError
_ test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_stochastic_P.py] _

path = 'tests/chase/test_stochastic_P.py'

    @pytest.mark.parametrize("path", BARE_CONFTEST_IMPORTERS)
    def test_no_test_file_imports_conftest_as_a_bare_module(path: str):
        """The mechanism, per file, so a partial repair is visible as a partial pass."""
        found = resolve(path)
        if found is None:
            pytest.fail(
                f"{path} resolves to no file, at its own path or under attic/. "
                "A named offender that exists nowhere is not a repair; say where it went."
            )
        src = found.read_text(encoding="utf-8")
        # `.lstrip()` before matching. Without it this assertion saw only UNINDENTED
        # imports while `bare_conftest_importers()` scans with `^\s*`, so five of the
        # ten parametrised files were collected and could never fail -- the selector
        # and the check disagreeing about what counts, which makes half the cases
        # vacuous. A function-local `from conftest import X` resolves through
        # `sys.path` at call time exactly as a module-level one does.
        offenders = [
            line.strip()
            for line in src.splitlines()
            if line.lstrip().startswith(("from conftest import", "import conftest"))
        ]
>       assert not offenders, (
            f"{path} reaches conftest as a top-level module: {offenders}. "
            "With only 2 of 26 test dirs carrying __init__.py, that name resolves by "
            "collection order."
        )
E       AssertionError: tests/chase/test_stochastic_P.py reaches conftest as a top-level module: ['from conftest import HAS_CUDA']. With only 2 of 26 test dirs carrying __init__.py, that name resolves by collection order.
E       assert not ['from conftest import HAS_CUDA']

tests\loop\test_conftest_import_is_order_dependent.py:208: AssertionError
_ test_no_test_file_imports_conftest_as_a_bare_module[attic/tests/chase/test_multizoom_cost.py] _

path = 'attic/tests/chase/test_multizoom_cost.py'

    @pytest.mark.parametrize("path", BARE_CONFTEST_IMPORTERS)
    def test_no_test_file_imports_conftest_as_a_bare_module(path: str):
        """The mechanism, per file, so a partial repair is visible as a partial pass."""
        found = resolve(path)
        if found is None:
            pytest.fail(
                f"{path} resolves to no file, at its own path or under attic/. "
                "A named offender that exists nowhere is not a repair; say where it went."
            )
        src = found.read_text(encoding="utf-8")
        # `.lstrip()` before matching. Without it this assertion saw only UNINDENTED
        # imports while `bare_conftest_importers()` scans with `^\s*`, so five of the
        # ten parametrised files were collected and could never fail -- the selector
        # and the check disagreeing about what counts, which makes half the cases
        # vacuous. A function-local `from conftest import X` resolves through
        # `sys.path` at call time exactly as a module-level one does.
        offenders = [
            line.strip()
            for line in src.splitlines()
            if line.lstrip().startswith(("from conftest import", "import conftest"))
        ]
>       assert not offenders, (
            f"{path} reaches conftest as a top-level module: {offenders}. "
            "With only 2 of 26 test dirs carrying __init__.py, that name resolves by "
            "collection order."
        )
E       AssertionError: attic/tests/chase/test_multizoom_cost.py reaches conftest as a top-level module: ['from conftest import requires_triton']. With only 2 of 26 test dirs carrying __init__.py, that name resolves by collection order.
E       assert not ['from conftest import requires_triton']

tests\loop\test_conftest_import_is_order_dependent.py:208: AssertionError
_ test_no_test_file_imports_conftest_as_a_bare_module[attic/tests/chase/test_multizoom_kernel.py] _

path = 'attic/tests/chase/test_multizoom_kernel.py'

    @pytest.mark.parametrize("path", BARE_CONFTEST_IMPORTERS)
    def test_no_test_file_imports_conftest_as_a_bare_module(path: str):
        """The mechanism, per file, so a partial repair is visible as a partial pass."""
        found = resolve(path)
        if found is None:
            pytest.fail(
                f"{path} resolves to no file, at its own path or under attic/. "
                "A named offender that exists nowhere is not a repair; say where it went."
            )
        src = found.read_text(encoding="utf-8")
        # `.lstrip()` before matching. Without it this assertion saw only UNINDENTED
        # imports while `bare_conftest_importers()` scans with `^\s*`, so five of the
        # ten parametrised files were collected and could never fail -- the selector
        # and the check disagreeing about what counts, which makes half the cases
        # vacuous. A function-local `from conftest import X` resolves through
        # `sys.path` at call time exactly as a module-level one does.
        offenders = [
            line.strip()
            for line in src.splitlines()
            if line.lstrip().startswith(("from conftest import", "import conftest"))
        ]
>       assert not offenders, (
            f"{path} reaches conftest as a top-level module: {offenders}. "
            "With only 2 of 26 test dirs carrying __init__.py, that name resolves by "
            "collection order."
        )
E       AssertionError: attic/tests/chase/test_multizoom_kernel.py reaches conftest as a top-level module: ['from conftest import DEVICES, requires_triton']. With only 2 of 26 test dirs carrying __init__.py, that name resolves by collection order.
E       assert not ['from conftest import DEVICES, requires_triton']

tests\loop\test_conftest_import_is_order_dependent.py:208: AssertionError
_ test_no_test_file_imports_conftest_as_a_bare_module[attic/tests/chase/test_multizoom_r5.py] _

path = 'attic/tests/chase/test_multizoom_r5.py'

    @pytest.mark.parametrize("path", BARE_CONFTEST_IMPORTERS)
    def test_no_test_file_imports_conftest_as_a_bare_module(path: str):
        """The mechanism, per file, so a partial repair is visible as a partial pass."""
        found = resolve(path)
        if found is None:
            pytest.fail(
                f"{path} resolves to no file, at its own path or under attic/. "
                "A named offender that exists nowhere is not a repair; say where it went."
            )
        src = found.read_text(encoding="utf-8")
        # `.lstrip()` before matching. Without it this assertion saw only UNINDENTED
        # imports while `bare_conftest_importers()` scans with `^\s*`, so five of the
        # ten parametrised files were collected and could never fail -- the selector
        # and the check disagreeing about what counts, which makes half the cases
        # vacuous. A function-local `from conftest import X` resolves through
        # `sys.path` at call time exactly as a module-level one does.
        offenders = [
            line.strip()
            for line in src.splitlines()
            if line.lstrip().startswith(("from conftest import", "import conftest"))
        ]
>       assert not offenders, (
            f"{path} reaches conftest as a top-level module: {offenders}. "
            "With only 2 of 26 test dirs carrying __init__.py, that name resolves by "
            "collection order."
        )
E       AssertionError: attic/tests/chase/test_multizoom_r5.py reaches conftest as a top-level module: ['from conftest import DEVICES, requires_triton']. With only 2 of 26 test dirs carrying __init__.py, that name resolves by collection order.
E       assert not ['from conftest import DEVICES, requires_triton']

tests\loop\test_conftest_import_is_order_dependent.py:208: AssertionError
_______ test_some_countable_unit_of_the_corpus_equals_the_readme_figure _______

    def test_some_countable_unit_of_the_corpus_equals_the_readme_figure():
        """The README cannot be repaired by picking a different unit. Measured.
    
        The obvious rescue for "a 20,000-line head" against a 211,766-line file is that
        "line" was written where "story" was meant -- TinyStories entries are
        multi-paragraph, and 211,766/20,000 = 10.6 lines per story is plausible. It does
        not survive measurement. Nothing in the file counts to 20,000:
    
            lines                          211,766
            blank-line-separated blocks    105,095
            double-blank-separated blocks       16
            <|endoftext|> markers                0
    
        So the corpus carries no story boundary in its own bytes, and no re-reading of
        the README's unit makes its figure true. Whoever fetched it applied a slice --
        almost certainly `dataset['train'][:20000]` over HuggingFace records -- and wrote
        the result out in a form that does not preserve the record boundary. That slice
        is recoverable only from the producer, never from the artifact.
    
        THE CONSEQUENCE, and it is why this test exists rather than a note. Since the
        derivation cannot be checked against the file, `data/CHECKSUMS.sha256` is not
        hygiene -- it is the ONLY thing that can tell a correct re-fetch from a wrong
        one. Every published number that touched this corpus rests on that hash and on
        nothing else.
        """
        if not CORPUS.exists():
            pytest.skip("tinystories_20k.txt not fetched here")
        stated = re.search(r"([\d,]+)-line head", README.read_text(encoding="utf-8"))
        if not stated:
            pytest.skip("data/README.md no longer states a figure to check units against")
        claimed = int(stated.group(1).replace(",", ""))
        raw = CORPUS.read_text(encoding="utf-8", errors="replace")
        units = {
            "lines": len(raw.split("\n")),
            "blank-separated blocks": len([b for b in raw.split("\n\n") if b.strip()]),
            "double-blank blocks": len([b for b in raw.split("\n\n\n") if b.strip()]),
            "endoftext markers": raw.count("<|endoftext|>"),
        }
>       assert claimed in units.values(), (
            f"data/README.md states {claimed:,} but no countable unit of the corpus "
            f"equals it: {units}. The figure cannot be rescued by reading 'line' as "
            "'story' -- the file preserves no record boundary. The upstream slice is "
            "recoverable only from whoever ran it, and until then data/CHECKSUMS.sha256 "
            "is the only check a re-fetch has."
        )
E       AssertionError: data/README.md states 20,000 but no countable unit of the corpus equals it: {'lines': 211766, 'blank-separated blocks': 105095, 'double-blank blocks': 16, 'endoftext markers': 0}. The figure cannot be rescued by reading 'line' as 'story' -- the file preserves no record boundary. The upstream slice is recoverable only from whoever ran it, and until then data/CHECKSUMS.sha256 is the only check a re-fetch has.
E       assert 20000 in dict_values([211766, 105095, 16, 0])
E        +  where dict_values([211766, 105095, 16, 0]) = <built-in method values of dict object at 0x000001DD47A5A540>()
E        +    where <built-in method values of dict object at 0x000001DD47A5A540> = {'blank-separated blocks': 105095, 'double-blank blocks': 16, 'endoftext markers': 0, 'lines': 211766}.values

tests\loop\test_corpus_is_recoverable_and_verifiable.py:144: AssertionError
_________ test_the_readme_derivation_matches_the_corpus_it_describes __________

    def test_the_readme_derivation_matches_the_corpus_it_describes():
        """THE DEFECT. The only tracked recipe for this file describes a different file."""
        if not CORPUS.exists():
            pytest.skip("tinystories_20k.txt not fetched here")
        stated = re.search(r"([\d,]+)-line head", README.read_text(encoding="utf-8"))
        assert stated, "data/README.md no longer states a line count for tinystories_20k.txt"
        claimed = int(stated.group(1).replace(",", ""))
        actual = sum(1 for _ in CORPUS.open("rb"))
>       assert claimed == actual, (
            f"data/README.md says a {claimed:,}-line head; the corpus has {actual:,} lines "
            f"({actual / claimed:.1f}x). It is the only tracked instruction for recreating "
            "this file, and following it literally yields a corpus that is not the one every "
            "published number used. Repair by stating the exact upstream operation "
            "(split, revision, slice), not by editing the number to match."
        )
E       AssertionError: data/README.md says a 20,000-line head; the corpus has 211,766 lines (10.6x). It is the only tracked instruction for recreating this file, and following it literally yields a corpus that is not the one every published number used. Repair by stating the exact upstream operation (split, revision, slice), not by editing the number to match.
E       assert 20000 == 211766

tests\loop\test_corpus_is_recoverable_and_verifiable.py:161: AssertionError
_ test_no_corpus_instance_has_a_boundary_node_that_cannot_propagate[reproduce-n1024-d4-t0.95] _

row = {'bits_clearing_delta': 0, 'bits_moving_nothing': 1, 'bits_total': 80, 'boundary_nodes_with_no_interior_neighbour': 1, ...}

    @pytest.mark.parametrize("row", corpus_rows() or [None],
                             ids=lambda r: (r or {}).get("id", "no-census"))
    def test_no_corpus_instance_has_a_boundary_node_that_cannot_propagate(row):
        """THE DEFECT, one case per instance so a repair reads as a partial pass."""
        if row is None:
            pytest.skip("census absent; nothing to rule on")
        n = row.get(FIELD, 0)
>       assert n == 0, (
            f"{row.get('id')} (seed {row.get('seed')}, |B|={row.get('boundary_size')}) "
            f"has {n} boundary node(s) with NO interior neighbour, so a do() on such a "
            f"node cannot propagate: measured fd exactly 0.0 in every norm. "
            f"`r10_corpus_spec.admissible` checks the MIRROR condition -- every "
            f"interior vertex reaches B, which is what makes (I - P_II) invertible -- "
            f"and nothing checks that every boundary vertex has an interior neighbour. "
            f"A corpus for causal consequence emitted an instance where a causal "
            f"intervention has none. Route: add the mirror clause to `admissible` "
            f"(every b in B has a neighbour outside B) and regenerate; this is RED "
            f"rather than fixed because the twelve measured rungs were measured on the "
            f"corpus as it stands."
        )
E       AssertionError: reproduce-n1024-d4-t0.95 (seed 0x3a14000a, |B|=80) has 1 boundary node(s) with NO interior neighbour, so a do() on such a node cannot propagate: measured fd exactly 0.0 in every norm. `r10_corpus_spec.admissible` checks the MIRROR condition -- every interior vertex reaches B, which is what makes (I - P_II) invertible -- and nothing checks that every boundary vertex has an interior neighbour. A corpus for causal consequence emitted an instance where a causal intervention has none. Route: add the mirror clause to `admissible` (every b in B has a neighbour outside B) and regenerate; this is RED rather than fixed because the twelve measured rungs were measured on the corpus as it stands.
E       assert 1 == 0

tests\loop\test_every_boundary_node_can_propagate.py:127: AssertionError
____________ test_no_weight_record_omits_a_declared_identity_field ____________

    def test_no_weight_record_omits_a_declared_identity_field():
        """The blast radius, over a sample. Scoped to the sample by construction."""
        records = sorted((ROOT / "results").rglob("*.pt"))[:SAMPLE]
        if not records:
            pytest.skip("no weight records fetched here")
        offenders: dict[str, int] = {}
        seen = 0
        for p in records:
            try:
                rec = load(p)
            except Exception:
                continue
            if not isinstance(rec, dict) or "state_dict" not in rec:
                continue
            seen += 1
            for f in absent_fields(rec):
                offenders[f] = offenders.get(f, 0) + 1
        assert seen, "no loadable weight records in the sample; the check is vacuous"
>       assert not offenders, (
            f"over {seen} records, declared CONFIG_FIELDS are absent: "
            + ", ".join(f"{f} in {n}/{seen}" for f, n in sorted(offenders.items()))
            + ". A field declared to make two objects different, that no producer writes, "
            "makes no two objects different."
        )
E       AssertionError: over 60 records, declared CONFIG_FIELDS are absent: device in 60/60. A field declared to make two objects different, that no producer writes, makes no two objects different.
E       assert not {'device': 60}

tests\loop\test_manifest_refuses_an_absence_it_has_not_earned.py:186: AssertionError
_______ test_the_bar_control_is_not_scored_on_the_tensor_it_trained_on ________

    def test_the_bar_control_is_not_scored_on_the_tensor_it_trained_on():
        """THE DEFECT. RED while the control is in-sample.
    
        Green once `calibrate_bar` scores `trained_two_feature` on a draw it did not
        optimise against -- which is what `rips_gate.fit_eval` already does for the
        decoder gate in the same tree.
        """
        src = function_source(SOURCE, FUNC)
        trained = tensors_trained_on(src)
        scored = tensors_scored_on(src)
        shared = trained & scored
>       assert not shared, (
            f"{FUNC} trains the positive control on {sorted(trained)} and scores it on "
            f"{sorted(scored)} -- sharing {sorted(shared)}. The clause "
            f"`{CONTROL_KEY} < 1.0` certifies a task as LEARNABLE from an IN-SAMPLE "
            "reading, while every arm it gates must clear 1.0 OUT-OF-SAMPLE "
            "(m3_capability.py:206, eval batch at seed+12345). The control is solving "
            "an easier problem than the one it certifies. rips_gate.py:163-168 states "
            "the rule this breaks, in this repo's own words, for the other gate: 'an "
            "in-sample reading would credit memorisation as decoding'. Route: score on "
            "a fresh draw; every block then needs re-certifying and bar_verdict's "
            "threshold may need revisiting, since a held-out control reads higher."
        )
E       AssertionError: calibrate_bar trains the positive control on ['feats'] and scores it on ['feats'] -- sharing ['feats']. The clause `trained_two_feature < 1.0` certifies a task as LEARNABLE from an IN-SAMPLE reading, while every arm it gates must clear 1.0 OUT-OF-SAMPLE (m3_capability.py:206, eval batch at seed+12345). The control is solving an easier problem than the one it certifies. rips_gate.py:163-168 states the rule this breaks, in this repo's own words, for the other gate: 'an in-sample reading would credit memorisation as decoding'. Route: score on a fresh draw; every block then needs re-certifying and bar_verdict's threshold may need revisiting, since a held-out control reads higher.
E       assert not {'feats'}

tests\loop\test_the_bar_control_is_scored_out_of_sample.py:137: AssertionError
============================== warnings summary ===============================
tests/loop/test_no_module_writes_a_file_at_import.py::test_module_has_no_import_time_write_or_argv_read[attic/scale/lo_probe.py]
  <unknown>:1: DeprecationWarning: invalid escape sequence '\_'

tests/loop/test_no_struck_constant_ships.py::test_no_struck_value_is_reachable_in_the_shipped_costs_dict
  <frozen importlib._bootstrap>:241: DeprecationWarning: builtin type SwigPyPacked has no __module__ attribute

tests/loop/test_no_struck_constant_ships.py::test_no_struck_value_is_reachable_in_the_shipped_costs_dict
  <frozen importlib._bootstrap>:241: DeprecationWarning: builtin type SwigPyObject has no __module__ attribute

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ===========================
FAILED tests/loop/test_conftest_import_is_order_dependent.py::test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_ceq_hub_package.py]
FAILED tests/loop/test_conftest_import_is_order_dependent.py::test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_hf_shipping.py]
FAILED tests/loop/test_conftest_import_is_order_dependent.py::test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_hub_package_hardening.py]
FAILED tests/loop/test_conftest_import_is_order_dependent.py::test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_kernel_contracts.py]
FAILED tests/loop/test_conftest_import_is_order_dependent.py::test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_rollback_flex_attention.py]
FAILED tests/loop/test_conftest_import_is_order_dependent.py::test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_schedule_rebuild.py]
FAILED tests/loop/test_conftest_import_is_order_dependent.py::test_no_test_file_imports_conftest_as_a_bare_module[tests/chase/test_stochastic_P.py]
FAILED tests/loop/test_conftest_import_is_order_dependent.py::test_no_test_file_imports_conftest_as_a_bare_module[attic/tests/chase/test_multizoom_cost.py]
FAILED tests/loop/test_conftest_import_is_order_dependent.py::test_no_test_file_imports_conftest_as_a_bare_module[attic/tests/chase/test_multizoom_kernel.py]
FAILED tests/loop/test_conftest_import_is_order_dependent.py::test_no_test_file_imports_conftest_as_a_bare_module[attic/tests/chase/test_multizoom_r5.py]
FAILED tests/loop/test_corpus_is_recoverable_and_verifiable.py::test_some_countable_unit_of_the_corpus_equals_the_readme_figure
FAILED tests/loop/test_corpus_is_recoverable_and_verifiable.py::test_the_readme_derivation_matches_the_corpus_it_describes
FAILED tests/loop/test_every_boundary_node_can_propagate.py::test_no_corpus_instance_has_a_boundary_node_that_cannot_propagate[reproduce-n1024-d4-t0.95]
FAILED tests/loop/test_manifest_refuses_an_absence_it_has_not_earned.py::test_no_weight_record_omits_a_declared_identity_field
FAILED tests/loop/test_the_bar_control_is_scored_out_of_sample.py::test_the_bar_control_is_not_scored_on_the_tensor_it_trained_on
15 failed, 501 passed, 3 warnings in 42.49s
sys:1: DeprecationWarning: builtin type swigvarlink has no __module__ attribute

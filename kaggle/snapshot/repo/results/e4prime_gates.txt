E4' GATE DEMONSTRATIONS -- RUN RECORD
=====================================
Run by CAMERON-2, 2026-08-26, Windows PowerShell 5.1, Python 3.11.9,
branch feat/epsth-prime work tree at commit 1c56985 + the one test fix in
tests/cameron/test_e4prime_registration.py (endpoint half reads its pair off
the drawn instance instead of the bridge edge; scale/negation_scope.py is
untouched). RUN class: every number below was produced live in this session;
nothing is copied from an earlier journal.

RUNNER 1 -- the task-level registration battery (the registered task's own
gates: truncation gate (a), decoder gate (b) with planted controls, scale
clause, executable oracle, hop reading).

    python -m pytest tests\cameron\test_e4prime_registration.py -v

    6 passed in 0.48s:

    test_e4prime_is_registered_with_house_shape ................ PASSED
        e4prime sits in M3_TASKS as the 4-tuple
        (make_e4prime_batch, e4prime_oracle, e4prime_features,
        e4prime_flipper_dependence); flipper dependence exactly 0.0.
    test_the_label_is_recomputed_from_x_and_moves_with_its_do_bit PASSED
        y equals the executable oracle bitwise on a drawn 256-example batch,
        and negating the CH_BRIDGE do()-bit on half the examples moves
        exactly those labels.
    test_admissibility_scale_is_enforced_at_generation ......... PASSED
        make_e4prime_batch raises ValueError at s=64 and s=512; the spec's
        pre-bridge partition is [32, 30], the measured 30 x 32 merge.
    test_gate_a_truncation_fires ............................... PASSED
        TRUNCATION GATE on instances read back out of the tensor markers:
        k=0 rung sqrt(2) = 1.414214, k=1 rung above the mean predictor
        (> 1.0), monotone tightening, exact 0.0 at the full budget.
    test_gate_b_strikes_every_local_radius_and_the_planted_control_fires
        ......................................................... PASSED
        DECODER GATE, both halves: static local features stay >= FAIL_BAR
        (0.9) out to radius 3 and > 0.75 at radius 5, while on identical
        instances/features/split the planted degree-sum label reads < 1e-6
        and the planted balanced degree-median < 0.70, gap > 0.30; both
        classes nonempty, planted label sd > 0.
    test_the_label_is_not_a_closed_form_of_any_bounded_neighbourhood PASSED
        k-hop reading >= 1.0 through k=8, > 0.75 at k=16, monotone, exactly
        0.0 once the budget covers every drawn pair; endpoint half on one
        drawn do()-on instance: the drawn left-x-right pair is NOT adjacent
        (hop distance > 1), reading -1 at one hop short of that distance and
        +1 exactly at it.

RUNNER 2 -- the substrate-level gate report over ceq.rips draws, which
measured results/e4_gate.txt originally (python -m scale.rips_gate). The two
REROUTE rows are E4''s own substrate; rerun output today:

LargestJoin_S2Rips_64  [REROUTE]   bridge=(21, 55)   merged 8 x 10
   gate (a) k-hop reachability: k=0:1.4142  k=1:1.4031  k=2:1.3571  k=4:1.0411
        k=8:0.0000  k=16:0.0000  k=32:0.0000  k=64:0.0000  k=128:0.0000
   gate (b) degree only  NRMSE=1.0019   <-- SPEC
   gate (b) +1hop 0.9130   +2hop 0.5474   +3hop 0.2185   +5hop 0.0055
   PLANTED degree-sum 0.0000   PLANTED degree-median 0.4826
   -> still leaks at radius 5 (0.0055): INADMISSIBLE, matching
      results/e4_gate.txt:44; the builder refuses it (scale clause).

LargestJoin_S2Rips_1024 [REROUTE]   bridge=(343, 354)   merged 30 x 32
   gate (a) k-hop reachability: k=0:1.4142  k=1:1.4128  k=2:1.4087  k=4:1.3947
        k=8:1.2694  k=16:0.7943  k=32:0.0000  k=64:0.0000  k=128:0.0000
   gate (b) degree only  NRMSE=1.0001   <-- SPEC
   gate (b) +1hop 1.0018   +2hop 1.0035   +3hop 0.9951   +5hop 0.8220
   PLANTED degree-sum 0.0000   PLANTED degree-median 0.5530
   -> ADMISSIBLE: leak closed 0.1565 -> 0.9951 at radius 3 versus the struck
      CriticalLarge_S2Rips_1024 row of the same run; control fires on the
      same instances (planted sum exact, median 0.5530 below its 0.70 floor,
      connectivity-vs-planted gap 1.0001 - 0.5530 = 0.4471). Bitwise equal to
      results/e4_gate.txt:47-54.

PROVENANCE NOTE. The bridge edge reproduced live is (343, 354), identical to
results/e4_gate.txt:47, so the seed path through sample_sphere/rips_edges/
_add_critical_bridge(join="largest") is unchanged since the original
measurement; no drift between the recorded gates and today's code.

OWED: nothing under this item. Both demonstration runners existed already
(the pytest battery shipped with the registration; python -m scale.rips_gate
shipped with the measurement); no new runner was built.

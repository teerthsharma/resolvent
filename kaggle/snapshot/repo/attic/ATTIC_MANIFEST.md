# ATTIC MANIFEST

Round 10, PHASE 0, iteration 4. The 32 rows `AUDIT.md` disposes ATTIC, moved by `git mv` with
relative paths preserved: a row at `<original path>` now lives at `attic/<original path>`.

`AUDIT.md` keeps naming every row by its ORIGINAL path, because a census is of a commit and
re-pathing the sheet would break the it.2 draw's reproduction against `AUDIT.md@06a180c`.
This manifest is the mapping. Once a file is in `attic/`, the reason below is the only record
anyone will read, so each is the reason as it stands AFTER the it.4 repair, not as first written.

**Repaired at it.4:** 8 rows printed presumption P1 -- withdrawn at it.1 -- verbatim as their reason.
Each is re-stated in the rule that actually governs it (P1'' plus the measured conjunction).
No disposition moved on the repair. One row, `scale/vgpe_flops.py`, was RE-CLASSED live/KEEP when
leg 2 of its P3 conjunction was falsified, and is therefore NOT in this manifest and NOT moved.

**Not moved, by L-G2:** nothing under `results/`, nothing under `ceq/hf_artifact/`, no
`tests/chase/*.jsonl`. Journals and published tables never move. The G2 gate over the 263 tracked
artifacts in `results/G2_BASELINE_it4.sha256` is byte-equal across this move.

| original path | new path | class | reason (repaired) |
|---|---|---|---|
| `tests/cameron/severed_fraction.py` | `attic/tests/cameron/severed_fraction.py` | orphan | helper: 0 test functions, 0 python importers, no journal; see KILL 3 |
| `tests/cameron/test_arc_reality.py` | `attic/tests/cameron/test_arc_reality.py` | vacuous | VACUOUS-BY-SCOPE under P1'' -- reason cell RE-STATED at r10 it.4. The cell it replaces printed presumption P1, WITHDRAWN at r10 it.1 (P1 keys on WHO built the input; every planted positive is constructed, so it fires on cures as readily as diseases). Governing ground, measured not asserted: 0 front-door calls to any production builder at the pinned census commit 06a180c (results/p1prime_rows.json, aliases=[]), so the plant enters BELOW the instrument's front door and the stages real input traverses go unexercised -- MISTAKES.md V-14, which is what P1' actually says. The P1'' carve-out does not lift it: 0 `test_claim_*` nodes and no external mechanism reimplemented from a cited source's defining equations. Third leg: 0 of its 0 6-significant-digit readings reproduce from any results/*.jsonl at abs=5e-7. ran ALL-GREEN in the iteration-1 sweep, which is exactly what a vacuous control does and is therefore not evidence either way. A CONJUNCTION, not one rule; see KILL 2 |
| `tests/cameron/test_hankel_mustfire.py` | `attic/tests/cameron/test_hankel_mustfire.py` | vacuous | VACUOUS-BY-SCOPE under P1'' -- reason cell RE-STATED at r10 it.4. The cell it replaces printed presumption P1, WITHDRAWN at r10 it.1. Governing ground: 0 front-door calls to any production builder at the pinned census commit 06a180c (results/p1prime_rows.json, aliases=[]), plus 0 of 0 6-significant-digit readings reproducing from any results/*.jsonl at abs=5e-7; 16 passed at the it.3 spot-check. THE `test_claim_*` LEG IS NOT LOAD-BEARING HERE and is struck from this row's ground: MERCURY measured that predicate blind at r10 it.2 (13 tests/foreman files carry an in-band RED banner, only 10 use the prefix), and this file spells its five refutation nodes `test_mustfire_*` plus a `test_selftest_*`. KILL WITH A ROUTE -- REROUTE, measured at r10 it.4 by HOUSE: with an off-by-one planted at ceq/hankel.py:143, tests/cameron/test_hankel_worked_example.py FIRED (12 failed, 14 passed, 4 xfailed) alongside this file's own 11 failed, 5 passed. ceq.hankel therefore retains a measured rejection region after the move, and the surviving calibration is tests/cameron/test_hankel_worked_example.py. See KILL 2 |
| `tests/chase/test_multizoom_cost.py` | `attic/tests/chase/test_multizoom_cost.py` | vacuous | VACUOUS-BY-SCOPE under P1'' -- reason cell RE-STATED at r10 it.4. The cell it replaces printed presumption P1, WITHDRAWN at r10 it.1 (P1 keys on WHO built the input; every planted positive is constructed, so it fires on cures as readily as diseases). Governing ground, measured not asserted: 0 front-door calls to any production builder at the pinned census commit 06a180c (results/p1prime_rows.json, aliases=[]), so the plant enters BELOW the instrument's front door and the stages real input traverses go unexercised -- MISTAKES.md V-14, which is what P1' actually says. The P1'' carve-out does not lift it: 0 `test_claim_*` nodes and no external mechanism reimplemented from a cited source's defining equations. Third leg: 0 of its 0 6-significant-digit readings reproduce from any results/*.jsonl at abs=5e-7. NOT RUN in the iteration-1 sweep (does not collect as a set). A CONJUNCTION, not one rule; see KILL 2 |
| `tests/chase/test_multizoom_kernel.py` | `attic/tests/chase/test_multizoom_kernel.py` | vacuous | VACUOUS-BY-SCOPE under P1'' -- reason cell RE-STATED at r10 it.4. The cell it replaces printed presumption P1, WITHDRAWN at r10 it.1 (P1 keys on WHO built the input; every planted positive is constructed, so it fires on cures as readily as diseases). Governing ground, measured not asserted: 0 front-door calls to any production builder at the pinned census commit 06a180c (results/p1prime_rows.json, aliases=[]), so the plant enters BELOW the instrument's front door and the stages real input traverses go unexercised -- MISTAKES.md V-14, which is what P1' actually says. The P1'' carve-out does not lift it: 0 `test_claim_*` nodes and no external mechanism reimplemented from a cited source's defining equations. Third leg: 0 of its 0 6-significant-digit readings reproduce from any results/*.jsonl at abs=5e-7. NOT RUN in the iteration-1 sweep (does not collect as a set). A CONJUNCTION, not one rule; see KILL 2 |
| `tests/chase/test_multizoom_r5.py` | `attic/tests/chase/test_multizoom_r5.py` | vacuous | VACUOUS-BY-SCOPE under P1'' -- reason cell RE-STATED at r10 it.4. The cell it replaces printed presumption P1, WITHDRAWN at r10 it.1 (P1 keys on WHO built the input; every planted positive is constructed, so it fires on cures as readily as diseases). Governing ground, measured not asserted: 0 front-door calls to any production builder at the pinned census commit 06a180c (results/p1prime_rows.json, aliases=[]), so the plant enters BELOW the instrument's front door and the stages real input traverses go unexercised -- MISTAKES.md V-14, which is what P1' actually says. The P1'' carve-out does not lift it: 0 `test_claim_*` nodes and no external mechanism reimplemented from a cited source's defining equations. Third leg: 0 of its 0 6-significant-digit readings reproduce from any results/*.jsonl at abs=5e-7. NOT RUN in the iteration-1 sweep (does not collect as a set). A CONJUNCTION, not one rule; see KILL 2 |
| `tests/foreman/test_window_width_is_the_missing_control.py` | `attic/tests/foreman/test_window_width_is_the_missing_control.py` | vacuous | VACUOUS-BY-SCOPE under P1'' -- reason cell RE-STATED at r10 it.4. The cell it replaces printed presumption P1, WITHDRAWN at r10 it.1 (P1 keys on WHO built the input; every planted positive is constructed, so it fires on cures as readily as diseases). Governing ground, measured not asserted: 0 front-door calls to any production builder at the pinned census commit 06a180c (results/p1prime_rows.json, aliases=[]), so the plant enters BELOW the instrument's front door and the stages real input traverses go unexercised -- MISTAKES.md V-14, which is what P1' actually says. The P1'' carve-out does not lift it: 0 `test_claim_*` nodes and no external mechanism reimplemented from a cited source's defining equations. Third leg: 0 of its 0 6-significant-digit readings reproduce from any results/*.jsonl at abs=5e-7. ran ALL-GREEN in the iteration-1 sweep, which is exactly what a vacuous control does and is therefore not evidence either way. A CONJUNCTION, not one rule; see KILL 2 |
| `tests/loop/test_theorem_hypotheses_hold_at_shipped_settings.py` | `attic/tests/loop/test_theorem_hypotheses_hold_at_shipped_settings.py` | vacuous | VACUOUS-BY-SCOPE under P1'' -- reason cell RE-STATED at r10 it.4. The cell it replaces printed presumption P1, WITHDRAWN at r10 it.1 (P1 keys on WHO built the input; every planted positive is constructed, so it fires on cures as readily as diseases). Governing ground, measured not asserted: 0 front-door calls to any production builder at the pinned census commit 06a180c (results/p1prime_rows.json, aliases=[]), so the plant enters BELOW the instrument's front door and the stages real input traverses go unexercised -- MISTAKES.md V-14, which is what P1' actually says. The P1'' carve-out does not lift it: 0 `test_claim_*` nodes and no external mechanism reimplemented from a cited source's defining equations. Third leg: 0 of its 9 6-significant-digit readings reproduce from any results/*.jsonl at abs=5e-7. ran ALL-GREEN in the iteration-1 sweep, which is exactly what a vacuous control does and is therefore not evidence either way. A CONJUNCTION, not one rule; see KILL 2 |
| `tests/w6/test_w6_attention.py` | `attic/tests/w6/test_w6_attention.py` | vacuous | VACUOUS-BY-SCOPE under P1'' -- reason cell RE-STATED at r10 it.4. The cell it replaces printed presumption P1, WITHDRAWN at r10 it.1 (P1 keys on WHO built the input; every planted positive is constructed, so it fires on cures as readily as diseases). Governing ground, measured not asserted: 0 front-door calls to any production builder at the pinned census commit 06a180c (results/p1prime_rows.json, aliases=[]), so the plant enters BELOW the instrument's front door and the stages real input traverses go unexercised -- MISTAKES.md V-14, which is what P1' actually says. The P1'' carve-out does not lift it: 0 `test_claim_*` nodes and no external mechanism reimplemented from a cited source's defining equations. Third leg: 0 of its 0 6-significant-digit readings reproduce from any results/*.jsonl at abs=5e-7. ran ALL-GREEN in the iteration-1 sweep, which is exactly what a vacuous control does and is therefore not evidence either way. A CONJUNCTION, not one rule; see KILL 2 |
| `tests/wilson/accum_divergence.py` | `attic/tests/wilson/accum_divergence.py` | orphan | helper: 0 test functions, 0 python importers, no journal; see KILL 3 |
| `scale/arm_s_batched.py` | `attic/scale/arm_s_batched.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 3 tracked file(s) (CHECKLIST.md, DONE.md), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/carpet_probe.py` | `attic/scale/carpet_probe.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 8 tracked file(s) (DONE.md, DONE_ARCHIVE_ROUND1.md), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/dfloor_probe2.py` | `attic/scale/dfloor_probe2.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 4 tracked file(s) (ARSENAL.md, CONTRACT.md), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/frustration_audit.py` | `attic/scale/frustration_audit.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 2 tracked file(s) (DONE.md, tests/cameron/test_the_signed_arm_is_signed_on_this_task.py), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/lastrow_bind.py` | `attic/scale/lastrow_bind.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 2 tracked file(s) (ceq/bench.py, scale/maskcache_bind.py), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/lo_probe.py` | `attic/scale/lo_probe.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 2 tracked file(s) (DONE_ARCHIVE_ROUND1.md, PREREGISTRATION_HOLE_AUDIT.md), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/maskcache_applied_bind.py` | `attic/scale/maskcache_applied_bind.py` | orphan | PRESUMED orphan: 0 python importers, 0 results artifacts, 0 prose citations in any tracked file. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/maskcache_bind.py` | `attic/scale/maskcache_bind.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 1 tracked file(s) (scale/maskcache_applied_bind.py), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/orbit_probe.py` | `attic/scale/orbit_probe.py` | orphan | PRESUMED orphan: 0 python importers, 0 results artifacts, 0 prose citations in any tracked file. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/r4b_units.py` | `attic/scale/r4b_units.py` | orphan | PRESUMED orphan: 0 python importers, 0 results artifacts, 0 prose citations in any tracked file. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/recall_probe.py` | `attic/scale/recall_probe.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 6 tracked file(s) (.superpowers/sdd/polymorphic-drifting-squirrel/progress.md, DONE_ARCHIVE_ROUND1.md), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/run_m2.py` | `attic/scale/run_m2.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 1 tracked file(s) (DONE_ARCHIVE_ROUND1.md), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/scale_sweep.py` | `attic/scale/scale_sweep.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 3 tracked file(s) (house-events.jsonl, tests/chase/axes.py), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/sgate_softmax_probe.py` | `attic/scale/sgate_softmax_probe.py` | orphan | PRESUMED orphan: 0 python importers, 0 results artifacts, 0 prose citations in any tracked file. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/sign_dependence_probe.py` | `attic/scale/sign_dependence_probe.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 1 tracked file(s) (DONE.md), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/sparse_probe.py` | `attic/scale/sparse_probe.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 4 tracked file(s) (.superpowers/sdd/polymorphic-drifting-squirrel/venus-report-it3.md, DONE_ARCHIVE_ROUND1.md), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/step_profile.py` | `attic/scale/step_profile.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 1 tracked file(s) (scale/sgate_softmax_probe.py), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `scale/window_sweep.py` | `attic/scale/window_sweep.py` | orphan | PRESUMED orphan: 0 python importers and 0 results artifacts. Named in prose by 2 tracked file(s) (tests/cameron/test_composition_is_the_uncosted_route.py, tests/foreman/test_window_width_is_the_missing_control.py), so a document rests on a producer nothing runs. PRESUMPTION, NOT VERDICT; see KILL 3 |
| `ADR-001-bucketed-measurement.md` | `attic/ADR-001-bucketed-measurement.md` | orphan | PRESUMED orphan: no other tracked file names this document or its stem (measured over 516 tracked text files). PRESUMPTION, NOT VERDICT; see KILL 4 |
| `titan-report.md` | `attic/titan-report.md` | orphan | PRESUMED orphan: no other tracked file names this document or its stem (measured over 516 tracked text files). PRESUMPTION, NOT VERDICT; see KILL 4 |
| `tests/mars/MARS_REPORT_IT3.md` | `attic/tests/mars/MARS_REPORT_IT3.md` | orphan | in-tree report artifact named by 0 other tracked files (measured over 516 tracked text files); see KILL 4 |
| `tests/mars/MARS_REPORT_IT4.md` | `attic/tests/mars/MARS_REPORT_IT4.md` | orphan | in-tree report artifact named by 0 other tracked files (measured over 516 tracked text files); see KILL 4 |

**32 files moved.**

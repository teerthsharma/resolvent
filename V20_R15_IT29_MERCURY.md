# V20 R15 · it.29 · MERCURY — THREE OF THIS OFFICE'S NUMBERS, WITHDRAWN OR RE-MEASURED

**All readings 2026-09-02, 13:03–13:12Z, local CPU, branch `v17k-gate0`.**
**This office wrote exactly three files: this one, `tests/mercury/screen_v20_r15_it29_numerics.py`,
and `tests/mercury/test_v20_r15_it29_withdrawals.py`.** No git write. Nothing touched Kaggle.

**Content digests (sha256[:16], raw bytes, at time of reading):**

| file | digest | read at |
|---|---|---|
| `V20_R15_THEORY_TABLE.md` | `942e4208893444cd` | 13:03:27Z, 13:07:25Z, 13:09:14Z — **it did not move** |
| `V20_R15_IT26_MERCURY.md` | subject of the `62` and the `0 of 22` | 13:04Z |
| `V20_R15_IT27_MERCURY.md` | subject of the `0 of 22` and the §4 sentence | 13:04Z |
| `tests/mercury/screen_v20_r15_it29_numerics.py` | `585123d359a43776` | written this iteration |
| `tests/mercury/test_v20_r15_it29_withdrawals.py` | `b2a9cc0fb1c46973` | written this iteration |

**`942e4208893444cd` is the digest it.27 recorded at 12:40Z and it.28 held.** The table has now
been still for two iterations. **Every count below is against that digest and against no other.**

## THE THREE, AND WHAT SETTLES EACH

| # | published | disposition | the command that settles it |
|---|---|---|---|
| 1 | `0 of 22` files under `tests/mercury/` are known to git | **WITHDRAWN → `9 of 25`** | `git ls-files tests/mercury \| wc -l` ; `ls tests/mercury/*.py \| wc -l` |
| 2 | *"§4 … asserts **not one numeric constant**"* | **WITHDRAWN → §4 asserts `9` integer-shaped quantities, `8` of them unaddressed** | `python tests/mercury/screen_v20_r15_it29_numerics.py V20_R15_THEORY_TABLE.md 282 392` |
| 3 | `29` uncited **of `62`** | **`62` WITHDRAWN → `63`** | `python tests/mercury/screen_v20_r15_it29_numerics.py V20_R15_THEORY_TABLE.md 126 228` |

**MARKER NODE: `tests/mercury/test_v20_r15_it29_withdrawals.py` — `4` RED / `4` GREEN**, every
RED against the unmutated tree, every RED going GREEN on a **document** edit and no code change.

**LANDINGS CENSUS: `34 → 39 of 129`.** Two clauses, `5 of 5` land. **`1` new defect, `M-29a`.**

---

## 1 — `0 of 22` → **`9 of 25`**, AND THE MECHANISM IS WORSE THAN THE NUMBER

```
[RUN] date -u                                                    2026-09-02T13:03:08Z
[RUN] ls tests/mercury/*.py | wc -l                           -> 25
[RUN] git ls-files tests/mercury | wc -l                      ->  9
[RUN] git ls-files tests/mercury
      test_r10_it19_pricing.py            test_r9_limits_e.py
      test_r9_eprocess_overflow.py        test_r9_mechanical_fixes.py
      test_r9_estimator_named.py          test_r9_seed_agreement.py
      test_r9_exact_interval.py           test_r9_table_cut.py
      test_r9_verdict_names.py
```

**`9 of 25` is the literal.** It is `9 of 27` at the close of this iteration — this office wrote
two more files under that directory — and the two readings are dated separately rather than
averaged. **The tracked half does not move either way, and the tracked half is the half the
withdrawn figure got wrong.**

**Where it was published, verbatim, so the withdrawal has an address:**

- `V20_R15_IT26_MERCURY.md:16-17` — *"MARS, it.25: `0 of 22` files under `tests/mercury/` are known to git"*
- `V20_R15_IT27_MERCURY.md:18` — *"**No HEAD SHA is published here** — `0 of 22` files under `tests/mercury/` are known to git."*

**THE MECHANISM: a figure that is TRUE of one directory was carried to a second and re-published
twice without re-measurement.** MARS's `0 of 22` was about **`tests/mars_v20`**, where it holds.
Nothing about it was wrong at its origin. It became wrong at the moment its subject was swapped,
and the swap was invisible because **the sentence around it did not change**. This office quoted
it under `[CITED: MARS]` discipline, and the citation discipline is exactly what carried it: a
correctly-attributed figure was assumed to be a correctly-*scoped* figure.

**WHAT IT CHANGES, AND WHAT IT DOES NOT.** The figure was load-bearing for one decision —
withholding a HEAD SHA from it.26 and it.27, on the ground that a SHA would not name the suite
printed beside it. **The decision survives, on a different and narrower ground:** all `9` tracked
files are `test_r9_*` / `test_r10_*`. **Not one file this round wrote is tracked.** A HEAD SHA
published beside an it.26 count would have named a tree containing zero of the instruments that
produced it — which is the original argument, restated on a fact that is true.

**And the corrected number makes the INSPECTOR's three tiers sharper, not softer.** `9` files can
be refuted. `18` — including every instrument this round built, and both files this office wrote
today — cannot. **The ratio is the round's tier-1 coverage under `tests/mercury/` and it is
`9 of 27`, not `0 of 22`.**

---

## 2 — §4. THE ZERO STANDS; THE SENTENCE BUILT ON IT IS WITHDRAWN

**The measurement is confirmed exactly.** §4 (`V20_R15_THEORY_TABLE.md:282-392`) contains **five**
float-shaped tokens and **all five are section references**:

```
[RUN] python tests/mercury/screen_v20_r15_it29_numerics.py V20_R15_THEORY_TABLE.md 282 392
      13:05Z   digest 942e4208893444cd
      FLOAT-SHAPED 5 :  :284 4.1   :319 4.2   :327 0.3   :333 4.3   :367 0.3
      INTEGER-SHAPED 112
```

**The sentence it.27 built on that zero is withdrawn:**

> *"§4 — the section that rules what the leap gets and what it must not be asked — asserts
> **not one numeric constant**. Its claims are files, functions, symbols and arms."*
> — `V20_R15_IT27_MERCURY.md:169-170`

**It is false, and it.27's own §8 had already conceded the ground it stands on** — that integers
sit outside every denominator that instrument uses. The honest replacement is not a softer
sentence, it is a sweep.

### 2.1 §4's INTEGER-SHAPED QUANTITIES, HAND-READ. **`9`, SCREENED FROM `112`.**

**REJECTION RULE, stated so it can be attacked.** Of the `112` integer-shaped tokens the screen
returns, `103` are rejected as **not quantities**: grade tokens (`F0`–`F4`), cell identifiers
(`Q2/W1`, `Q6/W3`), ledger and ruling ids (`L-2`, `L-9`, `L-13`, `L-14`, `J-14`, `V-it7`,
`M13`), iteration ids (`it.35`, `it.12`), bed names (`bed_1`), and mathematics (`1/d`).
**These are the same rejections it.28 published, applied to the integer half.**

**`9` survive. Every one is a quantity §4 asserts, and `8` of the `9` carry no `path:line`:**

| # | line | quantity | what it is | ruling |
|---|---|---|---|---|
| 1 | `:326` | **`~6 GPU-s`** | the price that "buys Q3/W3 outright" | **UNCITED** |
| 2 | `:331` | `0 GPU-s` | the price of Q1/W1's bookkeeping repair | **UNCITED** |
| 3 | `:331` | `0 of 24` | the attributability census | **UNCITED** |
| 4 | `:338` | `1 passed` | SATURN's detector result | **CITED** — by pytest node id, the correct addressing scheme for a test result (the it.28 `.jsonl`-discriminator rule) |
| 5 | `:353` | `0 GPU-s` | `L-2`'s hybrid verdict, quoted | **UNCITED** |
| 6 | `:354` | **`~275 GPU-s`** | `L-9`'s hybrid verdict, quoted | **UNCITED — and see 2.2** |
| 7 | `:371` | `0 of 24` | the census, restated as a surviving finding | **UNCITED** |
| 8 | `:372` | `s = 64` | `L-9`'s surviving constant | **UNCITED** |
| 9 | `:372` | `0 of 40` | `L-13`'s census | **UNCITED** |

> ### **§4: `8` UNCITED OF `9` ASSERTED INTEGER-SHAPED QUANTITIES, SCREENED FROM `112` TOKENS.**

**The `9` is not the `9` a reader might assemble from the section's prose.** Two of the nine are
`0 GPU-s` prices, which read as absences rather than quantities and are counted here because a
price of zero is the strongest claim a section about what the leap costs can make. **Anyone
recomputing this will disagree first about those two, and they are listed for that reason.**

### 2.2 `~275 GPU-s` AT `:354` IS A PRICE §2 OF THE SAME FILE ALREADY CORRECTS

```
[RUN] grep -n 275 V20_R15_THEORY_TABLE.md                                 13:08Z
      :184  ... this office's published `~275` **does not evaluate to 275**
            (its own formula gives `309.047`) and its `960` honest total is **34.8% high**
      :354  | `L-9` | *TERMINAL as a leap target, LEAPABLE by ~275 GPU-s and one argparse line* |
```

**The same file corrects the number at `:184` and re-asserts it at `:354`, 170 lines apart, with
nothing between them.** `:354` is inside the hybrid-verdict table — the table RULING J-14b exists
to resolve — so a leap reading §4 to learn *what it is being offered and at what price* reads a
price the table's own §2 has already withdrawn. **This is `M-25c`'s shape at the level of a
section rather than a cell: the correction exists in the file, and nothing addresses it.**

**The repair is one pointer and `0 GPU-s`: `:354` should read `~275 GPU-s`
(`V20_R15_THEORY_TABLE.md:184` — corrected to `309.047` there).** JUPITER's to apply; this office
does not edit the table.

---

## 3 — it.26's `62` IS WITHDRAWN. THE DENOMINATOR IS `63`.

**The screen is now a file and it returns `64`, not `63`:**

```
[RUN] python tests/mercury/screen_v20_r15_it29_numerics.py V20_R15_THEORY_TABLE.md 126 228
      13:04Z   digest 942e4208893444cd
      FLOAT-SHAPED 64          distinct (line, token) pairs: 62
      same-line repeats:  :141 `1e-6` (twice)   :221 `0.0` (twice)
```

`64` agrees with **it.27's re-implementation** (`§2 382/64`, `V20_R15_IT27_MERCURY.md:34`) and
with the nurse's independent reproduction. **it.26 published two operands and both are wrong:**

| it.26 `:138-140` | measured |
|---|---|
| `regex screen, float-shaped tokens only -> 63 tokens` | **`64`** |
| `rejected as a section reference -> 1` (`§0.3` @ `:180`) | **`1` — this one is right** |
| `read and ruled -> 62` | **`63` under it.26's own stated rule** |

**AND THE RECONSTRUCTION IS THE FINDING.** `62` is arithmetically reachable from `64`, but only
one way: **drop the two same-line repeats and do NOT apply the rejection it.26 names.**
`64 − 2 = 62`. There are three candidate arithmetics and they give three different numbers:

| rule | denominator |
|---|---|
| screen, minus the one named rejection (**it.26's own stated rule**) | **`63`** |
| screen, minus two same-line repeats, named rejection **not** applied | `62` — the published number, and the one arithmetic it.26's prose forbids |
| screen, minus the named rejection **and** the two repeats | `61` |

**So the second rejection is not merely unnamed — naming it would contradict the first.** The
number published is the one produced by a rule the report states and then does not use.
**`62` is withdrawn. `63` is published, with the rule that produces it and the command that
reproduces the `64` it comes from.**

**THE NUMERATOR DOES NOT MOVE. `29` STANDS — AND CANNOT BE REFUTED.** `§0.3` @ `:180` is not among
it.26's `29`, and neither repeat is: it.26's Q1/W3 row counts `1e-6` once and its Q6/W3 row does
not count `0.0`. **The reading is `29 uncited of 63`.** But `29` is a hand ruling whose per-token
list was never written to a file — **tier 3 under the INSPECTOR's ruling, and it stays there.**
This office can settle its own denominator and cannot settle its own numerator, because the
instrument that produced it no longer exists. **That asymmetry is the whole argument for tier 1**,
and it is being paid for here by the office that made the argument.

---

## 4 — LANDINGS CENSUS. **`34 → 39 of 129`. TWO CLAUSES. `5 of 5` LAND.**

**Clause A — §4.1's admission-condition table, table lines `282-318`. Clause B — §4.3's rule and
convention citations, table lines `335`, `346`, `347`, `368`.**

| table line | citation | what is there | verdict |
|---|---|---|---|
| `:289` | `V20_R15_IT12_INSPECTOR.md:272` | `### 2.2 STRIKE — "five F4 rows" is six.` | **LANDS** — the strike, and `:273-274` name the omitted `L-8` |
| `:335` | `V20_R15_IT567_INSPECTOR.md:482` | *"…**Naming a field and then"* | **LANDS** — the quoted rule opens on `:482` and runs to `:483`; the cite is to its first line |
| `:346` | `tests/saturn/test_v20_r15_it12_saturn.py:329` | `if head.startswith("none") or not head:` | **LANDS** — verbatim, the arm the table says the four rows trip |
| `:347` | `V20_R15_LEAP_LEDGER.md:99` | `TERMINAL rows name no field, which is the rule.` | **LANDS** |
| `:368` | `V20_R15_LEAP_LEDGER.md:130` | the `L-13` row, carrying the quoted FIELD text verbatim | **LANDS** |

**`0` FAIL. `0` weak.** `5` is added; **`34 → 39`. No rate and no extrapolation to `129` is offered.**

**TWO CITATIONS IN CLAUSE A ARE EXCLUDED FROM THE COUNT AND NAMED HERE INSTEAD.** `:306` cites
`ceq/beds/bed_k.py:236` (`def build_delay`) and `ceq/kdata.py:475`
(`"bed_k": {"generator": "ceq.beds.bed_k.build_delay"`). **Both land.** Both are the `M-25c`
replacement **this office named at it.26** and JUPITER applied. **Checking one's own repair is not
an independent landing** and it is not counted as one.

**DISJOINTNESS, and the one place it is soft.** it.25's `21` (lines `71, 95, 104, 163–170, 171,
203, 304`), it.27's `+3` (`339, 379, 382`) and it.28's `+10` (`211–228`) contain none of the five
above. **But it.25's line numbers were taken against a different digest** — current `:304` carries
no citation at all — so disjointness is asserted here on **citation identity**, not line number:
none of `IT12_INSPECTOR.md:272`, `IT567_INSPECTOR.md:482`, `saturn.py:329`, `LEAP_LEDGER.md:99`,
`LEAP_LEDGER.md:130` appears in any published landings table this round.

### 4.1 **`M-29a` — THE Q2/W1 ADMISSION CONDITION ASSERTS A FALSE FACT**

`V20_R15_THEORY_TABLE.md:306`, the ADMISSION CONDITION under which the **F4 cell Q2/W1 re-enters
the it.35 gate**, reads: *"the generator is `build_delay` @ `ceq/beds/bed_k.py:236`, registered
@ `ceq/kdata.py:475`, and **has zero callers under `scripts/`**"*.

```
[RUN] grep -rn build_delay scripts/                                        13:08Z
      scripts/v20_m14_cheeger.py:347   b = bed_k.build_delay(n=500, d=4, seed=7)
      scripts/v20_m14_cheeger.py:462   b = bed_k.build_delay(n=500, d=4, seed=7)
      scripts/v20_m14_cheeger.py:349   (a label string, not a call)
      -> 2 call sites, not zero
```

**Both pointers land and the sentence they sit in does not.** This is a class distinct from every
one filed this round: **the citations are correct and the un-pointered clause between them is
false.** It costs more than an uncited constant because the false clause is the *reason* the cell
is NOT-PUT — §4.1 tells the leap that BED-K's shape has never been run in this tree, and
`scripts/v20_m14_cheeger.py` runs it twice, at `n=500, d=4, seed=7`, the registered kwargs at
`ceq/kdata.py:476` exactly.

**The repair is a sentence, not a run:** `:306` should say **what** the two call sites do and why
they are not an arena cell, or drop the clause. Whether `v20_m14_cheeger.py`'s draws satisfy the
admission condition is **not settled here** and is the open question `M-29a` leaves.

**AND THE INSTRUMENT ALMOST MISSED IT, FOR THE REASON §1 IS ABOUT.** The first version of the
`m29d` node used `git grep`. It returned **zero hits and went GREEN**, confirming the false claim
— because `scripts/v20_m14_cheeger.py` **is not tracked**. The node now walks `scripts/*.py`
directly. **A git-backed instrument reads an untracked tree as an empty one**, and this tree is
`9 of 27` tracked under `tests/mercury/` alone.

---

## 5 — MARKER NODE. **`4` RED / `4` GREEN.**

```
[RUN] python -m pytest tests/mercury/test_v20_r15_it29_withdrawals.py -q     13:09:14Z
      4 failed, 4 passed in 0.73s
      RED  : m29a_the_two_reports_do_not_carry_the_0_of_22_literal
             m29b_sec4_prices_carry_a_pointer
             m29c_it26_publishes_the_screen_count_the_screen_returns
             m29d_sec4_1_zero_callers_claim_is_true
      GREEN: m29a_control_nine_of_twentyfive_mercury_files_are_tracked
             m29b_control_sec4_screens_five_floats_and_112_integers
             m29c_control_sec2_screens_64_floats_of_which_62_are_distinct
             m29d_control_the_claim_is_where_this_office_says_it_is
```

`sha256[:16]`: `screen_v20_r15_it29_numerics.py` = `585123d359a43776`,
`test_v20_r15_it29_withdrawals.py` = `b2a9cc0fb1c46973`.

**`m29a` and `m29c` are RED against this office's own two reports and go GREEN when those reports
are corrected.** They are the only nodes this round that a MERCURY report can turn green by
admitting something. **`m29c_control` carries the whole reconciliation as three assertions** —
`64` raw, `62` distinct, `63` after the named rejection — so a later office disagreeing with the
withdrawal has one file to attack and does not have to reconstruct prose.

---

## 6 — WHAT THIS OFFICE DID NOT REACH

- **The `29` numerator was not re-derived.** Only its denominator is settled. **A re-derivation
  needs a hand read of `63` tokens and it was not done in the wall clock** — so `29 of 63` is
  tier 1 on the denominator and tier 3 on the numerator.
- **§0 and §1 have never been swept, by anyone**, for constants or for named artifacts. §4 is now
  swept on both populations, §2 on both, §3 and §5 on named artifacts only.
- **The CORRECTIONS INDEX `overturns:` field is still unconsulted** — flagged at it.28 and still
  open. **No count in this report subtracts anything the index may have killed.** The error is
  one-sided and these numbers are gross.
- **`M-29a` is filed, not resolved.** Whether `scripts/v20_m14_cheeger.py:347` and `:462`
  constitute "one cell of BED-K's shape actually run" — and therefore whether Q2/W1's admission
  condition is already satisfied — is the question, and answering it means reading that script.
- **it.26 and it.27 are NOT EDITED.** The withdrawals are published here and the two REDs stand
  against the original text. Rewriting a prior report to make a node green would defeat the node.

## 7 — LIMITS

Every count is against `V20_R15_THEORY_TABLE.md` @ `942e4208893444cd`, unmoved through 13:09:14Z;
a count quoted against another digest is another count. The `9` §4 quantities and the `29` §2
constants are hand rulings under rules stated in §2.1 above and in `V20_R15_IT26_MERCURY.md:145`;
the screens fix populations only. `9 of 25` is the 13:03:08Z reading and `9 of 27` the closing
one, both dated rather than reconciled. The two `0 GPU-s` prices are the disputable half of §4's
`9`. Landings disjointness rests on citation identity because it.25's line numbers were taken
against a digest this office cannot reproduce. `[CITED: MARS]` for `0 of 22` on `tests/mars_v20`,
which is not re-measured here and is not withdrawn. No git writes. Nothing touched Kaggle.

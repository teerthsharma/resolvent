# STATE — CEQ v8.2, ROUND 6, THE HILBERT ROUND

| field | value |
|---|---|
| contract | `LOOP_PROMPT.md` (v8.2). Round 5 archived `LOOP_PROMPT_ROUND5_ARCHIVE.md` |
| promise | **`HILBERT`** — D2 verbatim, or an honest `BROKEN` |
| iteration | **4 complete, 5 next** |
| phase | **A — gates and fetches (iterations 0–4)** |
| scoreboard | **4** — Star delta +2, κ<1 measured +2 *(qualified: Birkhoff route dead, structural β earned it)* |
| RULE 2 clock | **running.** M3 settled-vs-unsettled EXECUTES by **iteration 12** or breach review |
| register | **caveman, all agents, every iteration.** Artifacts stay normal English |
| autonomy | user meets the loop at **iteration 30**; no check-ins |

## THE ONE NEXT ACTION (round 6, iteration 5)

**Collect Cameron and Chase. Foreman is in; two of three fellows are still out.**

**EXIT GATE A stands at 3 of 4:** deltas filed (Star clear, Sinkhorn occupied,
Birkhoff-for-attention partially occupied, DEQ narrowed, Shapley readout open);
κ certified **by the structural route, not Birkhoff**; matcher and SPRT still out.

**When they land, read in this order:**
  1. **CAMERON's matcher calibration BOTH directions**, then the F-green matched
     re-run — **+3/−5, and it decides the round's shape.**
  2. **CHASE's SPRT calibration** below `r₀`, above `r₁`, between; and the M3
     synthetic dry-run both directions.

**Owed to Foreman, and it is now a build decision, not a question:**
  * **ARM S cannot ship float32.** 16861/523776 exact zeros on the allowed
    support at s=1024. Either float64 or log-domain, and **that is a wall-clock
    line item under 1.7 that nobody has costed.** K-F applies to the SUM.
  * **The consistency gate needs a third leg or it is decoration** — `0.1 ≤ 1.0`
    passes 30/30 and proves nothing. The structural β is the candidate.
  * **β is a knob**, and ideal 3 says scale-free by construction, never by
    tuning. β *is* scale-free but it *is* chosen. **Unadjudicated.**

**Still owed by anyone:** whether `Δ̂` shrinks at **trained** projections. Every
number in this round so far is random-init — the project's oldest open item.

## Open REDs

Carried from round 5, all still open:

1. **K1's sign-flip clause** — the only pre-registered kill left genuinely
   undecided. 8 events in 2400 draws, `0.003333 [0.001440, 0.006557]`; the
   unsigned arm's interval `[0, 0.010195]` **contains** it. **Round 6 closes it
   by SPRT (K-H, +1)** rather than by 20,000 fixed draws.
2. **Every probe number ever taken was random-init.** The trained-projection
   re-run is Phase D and is the oldest open item in the project.
3. **What peak attention tracks** once the key-norm confound is removed —
   retention 2.9% / 13.3% / 58.9%, matched by **rank not value**, so those are
   **upper** bounds.
4. **The provenance bind is RED deliberately** — nine shipped cost figures whose
   run evidence lives in `DONE_ARCHIVE_ROUND1.md`. Repair by **re-pointing**,
   never by pasting.

# CEQ v16 — ROUND 12: THE BANKING ROUND

Complete contract, filed verbatim from the author's message of 2026-08-31.
Supersedes v15.x; absorbs X₃₂–X₃₈. `CEQ_V15_CONTRACT.md` and the three v15
deltas stay verbatim-of-record under RUL-5 and are now historical.

**Bank the crossing, build the skyline, run the two-sided test, close the
theorems. NO NEW CONSTRUCTIONS — the round's first law and its own kill.**

---

## NORTH STAR (immutable)

Attention that is EQUAL to self-attention on its own ground, built FROM softmax
and AdamW, and capable on ground they cannot occupy — it predicts the NEXT STATE
toward equilibrium (which basin, whether at a decision point, which transition),
not the next token.

Last line of every iteration report: **distance to this sentence, and the
scoreboard line.**

---

## WHERE R11 LEFT US (binding)

- Scoreboard **4/38**.
- **C-PAR repaired to a containment theorem** — softmax is the `β=1, g≡0, QK-on`
  corner; linear attention is the `β=0` corner; the path product is the
  `β=0, QK-off` corner.
- **C-CAP 0 of 9 — but R1 read BIMODALLY:** five of eight seeds crossed a floor
  no arm had ever crossed, three diverged with `â_max` to `285`, **nothing
  between `0.663` and `1.113`**, CI straddles.
- **C-TS has a bed.**
- **Nine contract statements checked, SIX WRONG, ALL SIX OPTIMISTIC — a bias,
  not a noise level, and it belongs to the contract's author.**
- **M-18 and its prescribed replacement failed the same way, mirrored.**
- **Lean #2 was proved on `0<a<1` for a corpus drawn on `{−1,0,+1}`.**
- **The discriminating instrument was FOUND** (gate-`R²`: `0.699` trained vs
  `0.372` zero-step), **not prescribed.**

**Debts:** the approximation bound · R3's two inverse registrations · device
re-certification (637 CPU-h vs 47 GPU-h) · the scan skyline, correctly refused as
a new construction in R11 and **legal now as the native control**.

---

## STANDING LAWS

L-VALUE · L-SCOPE · L-BIND · L-PRIOR · L-G2 · L-FIRST · L-TOL · L-TIME · L-EQ
(`[V-eq]` = theorem WITH hypotheses AND a run instance) · L-LEAN (identity
theorems green before training) · D-1..D-4 — **and three NEW, each paid for in
R11:**

**L-DOM.** Every theorem that gates a run ships a **DOMAIN CENSUS**: the corpus's
actual value support printed against the theorem's hypotheses. **No overlap ⇒ the
theorem is decoration.**

**L-SIGN.** The contract author files a **COUNTER-PREDICTION of equal
specificity** beside every prediction, and a **calibration column** (statements
checked / wrong / sign of error) is kept across rounds. **A one-sided sign
discounts the author.**

**L-DIAG.** A contract may prescribe **what a diagnostic must distinguish**
(trained from untrained, planted from unplanted), **never which statistic does
it**. The statistic is found on data, then given its must-fire. *(M-18's lesson,
both halves.)*

---

## THE ROOM

One role each; planets are responsibilities, not processes.

| | |
|---|---|
| **JUPITER** | maths, Lean, fetches, derivations |
| **SATURN** | instruments, controls, manifests, `MISTAKES.md` |
| **MERCURY** | pricing (RULE 4), scheduling, execution, **the only tables** |
| **MARS** | standing adversary, ≤1 value-testable attack per iteration |
| **VENUS** | one competing numeric prediction per deciding measurement, committed before data; **scored both halves** |
| **NEPTUNE** | systems gate — calibrated sizing `3.9 tensors × 3.4 B/elt`, dispatch, wall-clock, **the device decision** |

---

## PART I — THE ARM AS IT NOW STANDS (§S-M′ with phase gates)

**GATE (X₃₆, the R11 leap).** `a_i = m_i · e^{iθ_i}`.
- Magnitude `m_i ∈ [0,1]` **CLOSED** — the band mask via the even feature
  (`φ(x)²` recovers the band at `R² = 1.000000`) with a hard cap, so `0` and `1`
  are attainable **VALUES**, not limits. *(R1's three divergent seeds were an
  open range chasing a boundary target.)*
- Phase `θ_i` on the circle — BED-M's Rademacher gates are `θ ∈ {0,π}`;
  `|Π a| = 1` exactly on the band `[RUN: 10⁴ phases → 1.000000000000]`; parity
  mask = the `Z₂` winding number `[RUN: identical]`.

**HOP (§S-M′).** `W_ij = exp((C_i − C_j) + q_i·k_j) / Z_i^β`, `j ≤ i`, with `C`
complex (`Σ log m + i Σ θ`); **three corners provable (Lean #5a)**; `β`, `QK`,
`g` switches learnable; **if training pins a corner on every instance the card
names the family the arm actually is.**

**CARRIER ALTERNATIVE (S3-R, X₃₂ corrected).** Replicator–mutator with injection
`μ·softplus(·)` (Poincaré–Hopf needs `μ>0`), `ρ_P` gate at **2** (antisymmetric
`J` reads 2 exactly; `√2` is random-matrix), `dP/dt = Var(f)` with `f̄ = 2P`; ESS
verdict certifies **STABLE, never GOOD**.

**MEMORY LANE (S-K).** Attention unchanged + the fractional head (GL weights, one
scalar `α`; binds `α→0` identity, `α→1` cumsum).

**LANE COUPLING (X₃₈, the R12 design answer).** fast→slow **only** through an
anti-aliased decimated summary (averaging theorem; leak must-fire `0.099` vs
`0.565`); slow→fast **only** as a BIAS on the gate logits (slaving);
precision-weighted fusion where both lanes estimate one quantity; WFQ shares with
a **starvation census**.

**GUARDS (S-G, X₃₃).** Isocommittor `q=½` surfaces; itinerary = actions; Pesin
deficit `λ̂ − h_sym` printed (`≈0` iff generating; **only the `≈0` test is
admissible** — deficit is non-monotone in misplacement).

**CERTIFICATES (X₃₇).** `Z` winding of the learned phases (integer or instrument
defect); persistent `β₁` of the carrier trajectory via the author's Rips toolkit
(recurrent iff `≥1`; RPS 1 vs coordination 0); **Euler–Poincaré:** `Σ(−1)^k β_k`
must equal the Poincaré–Hopf index sum of the equilibrium census.

**CONSERVATION CENSUS (S-C).** Carriers hold mass to `1e-12`; readouts print
their dissipation budget.

**CLAIM SHAPE: composition only.** Occupied components cited before named —
SSD/GLA/RetNet (decay masks), **LRU/unitary RNNs (phase gates — the CLOSED
magnitude is the whole delta, and it is small)**, MWU (replicator),
Mori–Zwanzig-in-ML, ARFIMA, clockwork/HM-RNNs and MoE routers (lane coupling),
symbolic-dynamics estimators, TDA-of-dynamics (Perea–Harer).

---

## PART II — THE LEAN LEDGER (green before train; **L-DOM on each**)

| # | statement | status |
|---|---|---|
| 1 | `chain_path_product` | [M] |
| 2 | `prefix_logit_mask` — **RE-STATED on the corpus's actual support**: magnitudes in `[0,1]` with the band's `0/1` handled by the even feature, phases on the circle, `log` only where `m>0` | [M] |
| 3 | `parity_sign` = `Z₂` winding | [M] |
| 4 | `resolvent_eq_path_sum` | [S] |
| 5a | `three_corners_containment` | [M] |
| 5b | `gate_zero_beta_zero_is_linear_attention` — **the refutation, so it cannot be re-claimed** | [M] |
| 6 | `bounded_gates_stable` | [M] |
| 7 | `scan_assoc` | [M] |
| 8 | `replicator_eq_cumsoftmax` | [M] |
| 9 | `walsh_two_point` + `multiplicative_epistasis_nonzero` | [M] |
| 10 | `hop_floor` | [D] |
| 11 | `lyapunov_gate_stationarity` | [D] |
| 12 | `first_order_cannot_delay` | [S] |
| 13 | `GL_weights_powerlaw` | [S] |
| 14 | `committor_eq_harmonic` | [S] |
| 15 | `resolvent_inverse_is_difference` | [M] |
| 16 | `unit_phase_product` | [M] |
| **17** | **NEW `approximation_bound`** — *"cannot represent"* → *"cannot fit within ε"*: for the monotone-gate family vs an even target, `inf` over the family of the `L²` error is bounded below by a computable constant — **the theorem that lets M-18's class read as a fit bound** | [S] |

**TRAIN-GATE:** `#1, #2(re-stated), #3, #5a, #6, #7, #16` green.

---

## PART III — THE BEDS

**BED-M** (Markov chain corpus): support census `{−1,0,+1}` printed against #2's
hypotheses; skyline = the gated scan at 4,769 matched (`m=93`); identity bind:
oracle gates in ⇒ label `≤ 1e-6`.

**BED-K** (memory kernels, registered this round): (a) pure delay `d`, (b)
power-law `K` (fBm-type); **R3's two inverse registrations RECONCILED BY THE
AUTHOR before any cell runs — a contract precondition, not a task**; positional
channel registered (a pure delay is positional — content-only encoding would fail
attention for a reason unrelated to kernels); Hurst covariate only with its
white-noise must-fire; `scipy.binom` NaN at `α=1` guarded; `α`-box refusal in
place.

**BED-1** (multi-basin): committor labels (splitting probability), never barrier
height; guards `q=½`; CK test, Morse census, Pesin deficit, conservation census.

**INTERVENTIONAL CHANNEL** on all beds: per-position `do()` bumps with oracle
responses; gates are Jacobians `[RUN 1e-9]`; `L_jac`.

**HIDDEN-CAUSE variant (X₃₅′)**: planted latent sources; detector = `(I−A)·residual`
exact, adjoint (time-reversal) in noise, Wiener when colored; **Kramers–Kronig
check SHIPS ONLY after its rebuilt must-fire (mine failed: `0.000` both cases)**;
Cramér–Rao floor per bed beside every onset CI.

---

## PART IV — THE DECIDING MEASUREMENTS

L-FIRST; **Venus AND the author's counter-prediction filed before each**; `N=8`;
resolution statements; every cell: manifest, floors, distance-to-skyline,
conservation drift, `λ̂`, unit-root flag, **gate-`R²`, `â_max`, `Z` winding**.

**R1′ THE RE-RUN.** BED-M, `t*=2`, `n=2048`, phase gates, same seeds.
- **PREDICTION:** 8/8 converge (`â_max ≤ 1` by theorem #16), interval no longer
  straddles, the 5/8 crossing **BANKS**.
- **COUNTER (the author's, filed against himself):** 8/8 converge but the
  crossing **shrinks to ≤3/8** because the magnitude cap removed gain the
  divergent seeds were exploiting — **stability bought at capability's price.**
- **Early-warning column:** `λ̂` and gate-`R²` from step 0 per seed; diverging
  seeds (if any) **must show `λ̂ > 0` before loss does**, or the boundary story is
  wrong and gate-`R²` says where.

**R2** BED-M, `t*=8`, `n=32768`: `floor₁ = 0.9354`; `ĥ > 1` for the first time —
`NRMSE < floor₁ ⟺ ĥ > 1`, **one event, paid ONCE: the R11 double-payment is
repealed.**

**R-SKY** the scan skyline read beside R1′/R2; **`Δ_sky` column mandatory**;
**beats-softmax sentences BANNED on BED-M** (softmax is a fellow approximator
there).

**R3** BED-K two-sided: scan-only `≥ 0.95` on the delay bed (theorem #12),
attention/fractional near 0, COMPOSED arm with X₃₈ within resolution of the
native primitive on BOTH beds.
- **COUNTER:** the anti-alias summary costs the delay bed the high-frequency
  content a delay IS — composed **loses on (a), matches on (b)**; then summary
  bandwidth is a swept dial, stated.

**R4** THE ARCHITECTURE TABLE: BED-M and BED-K side by side, each arm vs its
bed's native skyline; the honest sentence is whatever both columns permit.

**R5** BED-1: exit accuracy with committor labels; CK; Morse; Pesin deficit on
`q=½` guards; `β₁` recurrence certificate; the `(K̂, deficit)` policy —
point-predict / distribution-predict / refuse.

**R6** X₃₅′ hidden-cause localization vs the CRB floor at three noise levels;
**no-plant flatness (the scope control) first.**

**R7** Interventional ablation: `ζ>0` vs `ζ=0` at fixed `n`; prediction `≥2×`
fewer examples to R1′'s crossing; **counter: none — `L_jac` helps only where `a`
and `b` were confounded, which the interventional census may show they were
not.**

**MARS's filed attacks (it.0):** floor crossing by feature leak (Neumann census
on the reading tensors) · skyline leaks the oracle (gate values never in `x`) ·
guards chosen after itineraries (timestamp ordering) · parity by underpowering
(achieved-power column) · lane coupling starves a lane (share census) · **phase
gates win by parameter count** (the `±1` Rademacher target is cheap for a phase —
the param-matched even-feature-only control column).

---

## PART V — THE DEBTS

| debt | owner |
|---|---|
| **D-APPROX** Lean #17 / paper bound | JUPITER |
| **D-R3** the two inverse registrations | **THE AUTHOR, before R3** |
| **D-DEVICE** re-certification: 637 CPU-h vs 47 GPU-h is a 13× decision | NEPTUNE prices both with the calibrated model; **the round runs on the certified device only** |
| **D-CALIB** the calibration column: R11's nine statements, six wrong, sign `+`; **every R12 prediction discounted by it** | SATURN |
| **D-DOM** domain censuses on #2, #6, #12, #16 against BED-M/K | JUPITER |
| **D-KK** the Kramers–Kronig must-fire rebuilt in a frame with negative indices; a planted anticipating kernel reads nonzero or the instrument stays void | SATURN |

---

## PART VI — THE 40 ITERATIONS

| it | owner | work |
|---|---|---|
| 1 | SATURN | L-DOM/L-SIGN/L-DIAG into `MISTAKES.md` with R11's evidence; **the calibration column opened (9/6/+)** |
| 2 | JUPITER | Lean #2 re-stated on `{[0,1] magnitudes, circle phases}`; #16; **domain censuses printed for BED-M** |
| 3 | JUPITER | #1, #3, #5a, #5b, #6, #7 confirmed green under the re-statement; `lake build` exit 0 recorded |
| 4 | NEPTUNE | **D-DEVICE** — both devices priced, certificate signed for one; every later wall-clock cites it |
| 5 | SATURN | phase-gate arm built on §S-M′; identity binds (oracle gates ⇒ label `≤1e-6`; three corners bitwise on fp64 probes; `β`/`QK`/`g` switches present); manifests |
| 6 | VENUS + AUTHOR | R1′ predictions and counter-predictions filed **in separate commits**; MERCURY prices R1′/R2/R-SKY (RULE 4) |
| 7 | MERCURY | **R1′ — the re-run.** Early-warning columns live |
| 8 | ALL | R1′ verdict by floor with CI; branch — **BANKED (+12)** / split again (gate-`R²` diagnosis, **no new construction**) |
| 9 | SATURN | scan skyline built (`m=93`, 4,769), identity bind `≤1e-6`, manifest |
| 10 | MERCURY | R-SKY + R2 at `t*=8`; `Δ_sky` columns; `ĥ` column |
| 11 | ALL | BED-M verdict — the C-CAP sentence as permitted; **the R11 double-payment repealed in the scoreboard** |
| 12 | MARS | attacks 1–2 bound and run (feature leak; oracle leak) |
| 13 | AUTHOR | **D-R3 reconciled**; SATURN registers BED-K (positional channel, Hurst must-fire, `α` guards); admissibility battery |
| 14 | JUPITER | Lean #12 [S] attempted; D-APPROX draft (#17) |
| 15 | SATURN | X₃₈ lane coupling built: anti-alias summary path, bias injection, precision weights, WFQ shares; **the leak must-fire SEEN** (naive path leaks, filtered path ≤ baseline) |
| 16 | VENUS + AUTHOR | R3 predictions and counters filed |
| 17 | MERCURY | R3(a) delay bed — scan-only, attention-only, fractional, composed |
| 18 | MERCURY | R3(b) power-law bed — `α̂` recovered, `Ĥ = α̂ + ½` vs the calibrated estimator |
| 19 | ALL | R3 verdict; if the composed arm loses (a): the summary bandwidth sweep, **stated as a dial** |
| 20 | MERCURY | R4 — the architecture table cut (BED-M ∥ BED-K, each vs its native skyline) |
| 21 | MARS | attacks 3–5 (guards ordering; starvation census; parameter-count control for phase gates) |
| 22 | SATURN | X₃₇ certificates live — `Z` winding, `β₁` via the Rips toolkit (RPS/coordination must-fires), Euler–Poincaré check |
| 23 | SATURN | conservation census on every arm; unit-root flags |
| 24 | JUPITER | BED-1 committor labels + discrete Kramers rates derived; guards `q=½`; Pesin deficit instrument (rationals) |
| 25 | MERCURY | R5 — S5′ exit accuracy, CK, Morse, deficit, `β₁` |
| 26 | ALL | R5 verdict; the `(K̂, deficit)` policy table |
| 27 | SATURN | **D-KK rebuilt** (negative-index frame); must-fire seen |
| 28 | MERCURY | R6 — hidden-cause localization vs CRB at three noise levels; **no-plant flatness first** |
| 29 | SATURN | interventional channel live; MERCURY: R7 |
| 30 | ALL | R7 verdict; `L_jac` kept or dropped, stated |
| 31 | JUPITER | Lean #4, #13, #14, #15, #17 landed or priced; #10, #11 status stated |
| 32 | NEPTUNE | cost table on the certified device — decode bytes/token, dispatches, wall-clock vs FLOP |
| 33 | SATURN | probe battery through trained checkpoints (attribution `ω̂` vs `ω`, selection entropy, fan-out engagement, gate-`R²` over training) |
| 34 | MERCURY | New York demo on trained weights — the S5′ exit and its margin beside the founding-note figure |
| 35 | SATURN | HF package with real weights, limits-first card, every table inside; **upload on the author's explicit say-so only** |
| 36 | MARS | the next-class hunt on trained artifacts (**which control never saw a trained tensor?**) |
| 37 | VENUS | forecasting record — every filing vs outcome, both halves, **author's counters scored too** |
| 38 | SATURN | `MISTAKES.md` round entry; L-TIME labels; ledger adjudications; **the calibration column closed for R12** |
| 39 | JUPITER | D1 chapters — "additive attention cannot form path products," the phase-gate resolution of the unit-root split, the coupling law, **the taxonomy of the author's own errors** |
| 40 | ALL | prognosis — scoreboard, the one claim sentence the tables permit, next-X list. **STOP** |

---

## KILLS (immutable at first datum)

- **Any new construction ⇒ struck** (the round's own law).
- Train before the train-gate ⇒ discarded.
- **Theorem without domain census ⇒ inadmissible.**
- **Prediction without counter-prediction ⇒ not filed.**
- **Prescribed statistic in a diagnostic ⇒ M-18 class, struck.**
- **R1′ split again ⇒ NO new arm, gate-`R²` diagnosis only.**
- Skyline leaks oracle ⇒ `Δ_sky` void.
- Lane starvation ⇒ coupling redesign before any R3 sentence.
- KK/CRB numbers before their must-fires ⇒ struck.

---

## SCOREBOARD

Moves at it.8, 11, 19, 20, 26, 30, 40.

| event | pts |
|---|---|
| R1′ banked | +12 |
| R2 `ĥ>1` (**paid once**) | +4 |
| skyline + `Δ_sky` | +2 |
| BED-K registered | +2 |
| R3 composed matches native on both | +8 |
| R4 architecture table | +2 |
| R5 state model passes CK | +4 |
| R6 detector at CRB | +2 |
| R7 budget halved | +1 |
| certificates + conservation on every table | +1 |
| package | +3 |

**Ceiling 41 from the current 26%.**

**Floor:** a diagnosed second split with the instrument that explains it, and
D1's strongest chapters.
